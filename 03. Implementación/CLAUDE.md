# Bajoneá — Memoria de proyecto

Bajoneá es una plataforma web de pedidos gastronómicos para Río Grande, Tierra del Fuego. **A partir del 2026-08-27 el proyecto dejó de ser un MVP acotado y pasa a ser el proyecto completo** (ver §1bis) — cuatro roles (Cliente, Comercio/Dueño, Empleado, Administrador) sobre el modelo de datos completo de `docs/diccionario-de-datos.md` (v1.5, 41 tablas), con `bajonea_final` como base de datos definitiva. Las secciones de este archivo que todavía describen el alcance recortado original (§1, partes de §5 y de §6) quedan como **registro histórico** de cómo arrancó el proyecto — no como el alcance vigente.

Este archivo es la memoria persistente de proyecto para Claude Code. Se mantiene entre 300 y 500 líneas (excepcionalmente por encima mientras se reorganiza tras la enmienda de alcance) y se actualiza a medida que avanzan las fases. **No contiene el detalle histórico de decisiones** (eso vive en [docs/DECISIONES.md](docs/DECISIONES.md)) ni el detalle de columnas del modelo de datos (el modelo MVP original vive en `docs/modelo-mvp.md`; el modelo completo vigente vive en `docs/diccionario-de-datos.md` y `docs/bajonea_final.sql`).

**Fuente de verdad del plan de implementación original:** [docs/GUIA-IMPLEMENTACION-MVP-BAJONEA.pdf](docs/GUIA-IMPLEMENTACION-MVP-BAJONEA.pdf) (siempre revisar en esa ruta exacta) — cubre las Fases 0 a 18 ya cerradas o en curso, todas construidas sobre el modelo MVP. El plan de implementación de los tramos nuevos del proyecto completo (§1bis) todavía no existe como documento propio — se define en una sesión de planificación aparte con Diego.

---

## 1. Alcance del MVP original (histórico — alcance vigente en §1bis)

> **Esta sección describe el alcance con el que arrancó el proyecto (Fases 0-18) y se conserva como registro histórico, no como el alcance actual.** El detalle vinculante de por qué se acotó así en su momento sigue en la sección 0 de `GUIA-IMPLEMENTACION-MVP-BAJONEA.md` y en `docs/DECISIONES.md`. El alcance vigente desde el 2026-08-27 es el del **proyecto completo**, ver §1bis inmediatamente después de esta sección — en particular, la lista "Explícitamente fuera del MVP" de más abajo ya **no** describe exclusiones vigentes, son funcionalidades que ahora sí están en alcance y se van a implementar de a tramos.

**SÍ entraba al MVP:**
- Registro Cliente, Registro Comercio, Login (3 roles), Logout (3 roles).
- Verificación de cuenta por token real + email real vía SMTP.
- Aprobación/Rechazo de Comercio por Administrador.
- CRUD de Producto (Comercio) con galería de imágenes vía Cloudinary (hasta 5 imágenes, una principal).
- CRUD de Categoría y Tag (Administrador).
- Catálogo público de comercios y productos.
- Carrito simplificado: 1 registro por cliente, 1 comercio a la vez, sin expiración/timeout.
- Pedido simulado: nace directo en estado `PENDIENTE`, el comercio acepta o rechaza, se notifica in-app a cada parte.
- Direcciones con modelo completo (calle, número, piso/depto, código postal, localidad) y selector Provincia → Localidad dependiente, precargado por ETL desde la API Georef.
- Notificaciones solo in-app vía polling REST (sin email para pedidos).
- Autenticación JWT (jjwt) con rol embebido en el token. **Enmendado en la Fase 7** (ver `docs/DECISIONES.md`, entrada del 2026-07-17 "Enmienda formal de alcance del MVP"): dejó de ser puramente stateless — incorpora tabla `Sesion` (reincorporada del diccionario completo) y claim `sesionId` en el JWT, para poder invalidar sesiones de forma real en recuperación de contraseña, bloqueo de cuenta y login concurrente. `SessionCreationPolicy.STATELESS` de Spring Security se mantiene sin cambios (no hay `HttpSession`/cookie de servlet); lo que cambia es que ahora existe una tabla de sesión propia a nivel de aplicación, validada por `JwtAuthenticationFilter` en cada request.
- Recuperación de contraseña (token, 30 min), bloqueo de cuenta tras 3 intentos fallidos de login o de cambio de contraseña desde perfil, y reactivación de cuenta (token, 24hs) — los 3 sumados al MVP en la misma enmienda de la Fase 7. Sin tabla de historial de transiciones de estado (`HistorialEstadoUsuario`/`HistorialEstadoComercio` siguen fuera), sin suspensión por Administrador, sin job automático de inactivación por 3 meses.

**Explícitamente fuera del MVP en ese momento** (ver §1bis — ya no vigente, ahora en alcance): MercadoPago/Pago, WhatsApp, reclamos, soporte, reembolsos, jobs de expiración/timeout, suspensión de comercio/cliente por Administrador, historial de estados (`HistorialEstadoUsuario`/`HistorialEstadoComercio`/`HistorialEstadoPedido`), múltiples direcciones de cliente, horarios de atención granulares, `PENDIENTE_PAGO`.

**Figma:** se definió primero un checklist cerrado de 17 pantallas (Fase 15) y recién con eso aprobado se diseñó — no se tocaron entonces las pantallas del proyecto completo. La cobertura de Figma para los tramos nuevos del proyecto completo se define en la sesión de planificación mencionada en §1bis.

---

## 1bis. Alcance actual: el proyecto completo (enmienda del 2026-08-27)

**Decisión de Diego, no una evaluación técnica abierta a discusión** (ver `docs/DECISIONES.md`, entrada "Enmienda formal de alcance: el proyecto deja de ser un MVP acotado y pasa a ser el proyecto completo", 2026-08-27): el proyecto dejó de ser el MVP recortado de §1 y pasa a ser **el proyecto completo** — con Dueño, Empleado, red social del comercio, extras de producto, MercadoPago, Soporte/Reclamo, historial de estados y la máquina de estados completa de Pedido, entre otras piezas que §1 excluía explícitamente por nombre.

**Qué cambia en términos concretos:**

- **`bajonea_final` es la base de datos definitiva del proyecto** a partir de esta fecha — schema completo, 41 tablas, generado desde `docs/diccionario-de-datos.md` v1.5 (ver también `docs/bajonea_final.sql`). La base vieja (`bajonea`, migraciones Flyway `V1`-`V17`) queda **deprecada como base de trabajo activa**, no se migra in-place.
- El historial de Flyway `V1`-`V17` (aplicado contra `bajonea`) **no se re-arrastra**. La migración baseline nueva (`backend/src/main/resources/db/migration/V1__baseline_bajonea_final.sql`) ya existe y refleja el schema de `bajonea_final`. **Gap resuelto** (confirmado en el mini-tramo de infraestructura del 2026-09-02, ver `docs/DECISIONES.md`): la excepción detectada en la validación total del 2026-08-27 — la tabla `dueno` de este archivo sin la columna `persona_fisica_id` — ya no está presente; el archivo tiene hoy `persona_fisica_id` (con su `UNIQUE`/FK) igual que `docs/bajonea_final.sql` y la base física. No es un pendiente activo.
- **Portabilidad del backend hacia `bajonea_final` — Tramos 1 a 6 implementados y verificados en vivo, ninguno cerrado formalmente todavía** (ver `docs/DECISIONES.md`, entradas del 2026-08-27): Tramo 1 (baseline Flyway), Tramo 2 (`Usuario.foto_perfil_url`), Tramo 3 (`Token`: nuevo flujo de estado/intentos), Tramo 4 (`Pedido`: máquina de estados completa + reversión `detalle_rechazo`→`comentario_rechazo`), Tramo 5 (`Notificacion`: `entidad_tipo`/`entidad_id` en vez de `pedido_id`), Tramo 6 (`Comercio` → `Dueno`, cambio de cardinalidad de mayor riesgo). Hito alcanzado: el backend arranca 100% limpio contra `bajonea_final` por primera vez, con verificación end-to-end real (registro → aprobación → producto → pedido aceptado/rechazado → notificaciones, ambos roles) y `SELECT` directo contra las columnas críticas. Corrección adicional aplicada el mismo día: `Comercio.foto_perfil_url` (`NOT NULL` por diseño) ahora es exigida de verdad en el registro (`@NotBlank` en el DTO + validación de wizard en el frontend), cerrando un gap donde el registro fallaba con un `409` engañoso. Pendiente antes del cierre formal de estos 6 tramos: decisión de Diego sobre el gap de `V1__baseline_bajonea_final.sql` (arriba), actualización de la colección de Postman (desalineada con `bajonea_final` — usa el schema viejo), y confirmación explícita del checklist completo. `spring.datasource.url` ya apunta a `bajonea_final`.
- De los datos que había en `bajonea`, se migraron a `bajonea_final` únicamente `Categoria` (22 filas) y `Tag` (17 filas) — vía SQL directo, `docs/db/migracion-categorias-tags-a-bajonea-final.sql`, mismo criterio que el ETL de Georef (repetible, `ON DUPLICATE KEY UPDATE`, ids preservados). `Provincia` (24) y `Localidad` (4038) ya estaban cargadas en `bajonea_final` desde antes de esta migración, vía el mismo ETL de Georef. El resto de los datos de `bajonea` (comercios, productos, pedidos, usuarios de prueba) se descartó a propósito — eran datos de prueba, no de producción.
- **Las entidades que §1 excluía explícitamente del MVP ahora están en alcance:** `Dueño`, `Empleado`, `EmpleadoComercio`, `CuentaMercadoPago`, `ConfiguracionTarifa`, `HistorialEstadoUsuario`, `HistorialEstadoPedido`, `Soporte`, `Reclamo`, `Pago`, `NotaCredito` — más las tablas nuevas no mencionadas antes por nombre en §1 porque no existían como concepto todavía: `RedSocial`, `GrupoExtra`, `Extra`, `ProductoGrupoExtra`, `ItemCarritoExtra`, `DetallePedidoExtra`, y `usuario.foto_perfil_url`. Detalle tabla por tabla, capa por capa, en `docs/AUDITORIA-MODELO-DATOS-NUEVO.md` (2026-08-26).
- **El código actual NO se descarta.** El backend completo hasta la Fase 17 (24 Entities, 23 Repositories, 57 DTOs, 16 Services, 14 Controllers) y el frontend con las 87 pantallas del MVP anterior están **ya testeados end-to-end** (Postman, Playwright) y son la base funcional sobre la que se va a reestructurar todo, tramo por tramo, hasta que coincida con el modelo de datos nuevo — no es un punto de partida a reescribir de cero. El riesgo técnico de mayor magnitud identificado para esa reestructuración es el cambio de cardinalidad `Comercio ↔ Usuario` (hoy 1:1 vía `PersonaJuridica`, pasa a `Comercio → Dueño (N:1)`, con `Empleado` operando comercios ajenos) — afecta a la vez `ComercioService`, `ProductoService`, `PedidoService` y `AuthService` (ver `docs/AUDITORIA-MODELO-DATOS-NUEVO.md` §11).
- **`Dueño` y `RedSocial` ya están portados e implementados en el backend** (`entities/Dueno.java`, `entities/RedSocial.java`, más sus Repository/Service/Controller/DTOs correspondientes — `RedSocial` con su propio `RedSocialController`/`RedSocialService`) — no son tramos pendientes de planificación, ya están en código y funcionando, consistente con el Tramo 6 de portabilidad (`Comercio` → `Dueno`) mencionado arriba.
- **Orden e implementación de los tramos nuevos restantes:** no definidos todavía. Quedan listados, sin numerar ni ordenar, como pendientes de una sesión de planificación aparte con Diego: bloque de Extras (`GrupoExtra`/`Extra`/`ProductoGrupoExtra`/`ItemCarritoExtra`/`DetallePedidoExtra`, incluye tocar el cálculo de totales en `CarritoService`/`PedidoService`); `usuario.foto_perfil_url`; `Empleado`/`EmpleadoComercio` (cambio arquitectónico de mayor riesgo, toca JWT/autenticación); `CuentaMercadoPago`/`ConfiguracionTarifa`/Pago; `Soporte`/`Reclamo`; `HistorialEstadoUsuario`/`HistorialEstadoPedido`; máquina de estados completa de `Pedido` (11 valores del diccionario nuevo vs. los 3 recortados actuales). Ninguno de estos tramos tiene todavía número de fase asignado en §6 — se asigna recién cuando se planifique cada uno.
- **`spring.datasource.url` apunta a `bajonea_final`** (`application.properties`) — es la base definitiva del proyecto (ver el ítem sobre `bajonea_final` al inicio de esta lista).

---

## 2. Stack tecnológico

- **Backend:** Java 21, Spring Boot 3.x, Maven.
- **Persistencia:** MySQL + Flyway (`ddl-auto=validate`, nunca `update`/`create` — el esquema lo gestiona exclusivamente Flyway). Inspección visual vía phpMyAdmin.
- **Seguridad:** Spring Security + JWT (`jjwt`: api, impl, jackson), rol embebido en el token, `SessionCreationPolicy.STATELESS`.
- **Imágenes:** Cloudinary, subida firmada desde el frontend (el backend nunca recibe el binario).
- **Email:** Resend (API REST), vía `com.resend:resend-java`. Reemplaza a Brevo/SMTP (migración Fase 10, ver `docs/DECISIONES.md`, 2026-07-20) — `EmailService` ya no depende de `JavaMailSender`/Spring Mail.
- **Frontend:** HTML/CSS/JS vanilla (sin framework).
- **Documentación de API:** springdoc-openapi (Swagger UI).
- **Testing:** Postman (API) + Playwright (E2E).
- **Puerto:** `8080`, configurado explícitamente en `application.properties`. Nunca hardcodear el puerto en otro lugar.

---

## 3. Estructura de paquetes del backend (Fase 3)

```
com.bajonea.backend/
├── BajoneaApplication.java
├── config/
│   ├── security/
│   │   ├── SecurityConfig.java          <- cadena de filtros, reglas por endpoint
│   │   ├── JwtAuthenticationFilter.java
│   │   └── JwtService.java              <- generar/validar/parsear el token
│   ├── OpenApiConfig.java
│   ├── MailConfig.java                   <- bean del cliente Resend con la API key (Fase 10)
│   └── CloudinaryConfig.java            <- bean del cliente Cloudinary con credenciales
├── enums/                                <- HERMANO de entities/, no adentro (ajustado en la Fase 3)
│   ├── RolUsuario.java
│   ├── EstadoUsuario.java
│   ├── EstadoComercio.java
│   ├── EstadoProducto.java
│   ├── EstadoPedido.java
│   ├── TipoEntrega.java
│   ├── TipoToken.java                    <- ampliado a 3 valores en la Fase 7 (ver docs/DECISIONES.md)
│   ├── TipoCierreSesion.java             <- agregado en la Fase 7 junto con Sesion
│   ├── MotivoRechazo.java
│   ├── CondicionIva.java
│   ├── TipoPersonaJuridica.java
│   ├── TipoComercio.java
│   └── DiaSemana.java                    <- agregado en la Fase 16a junto con Horario
├── validation/                           <- agregado en la Fase 3, no estaba en el plan original
│   ├── annotations/                      <- @interface custom (ej. @ValidarCuit)
│   └── validators/                       <- ConstraintValidator correspondientes
├── dto/
│   ├── request/
│   │   ├── RegistroClienteRequestDTO.java
│   │   ├── RegistroComercioRequestDTO.java <- suma nombreRepresentante/apellidoRepresentante/dniRepresentante/telefonoRepresentante/fechaNacimientoRepresentante (corrección retroactiva a los Tramos 16.1/16.8, ver docs/DECISIONES.md)
│   │   ├── DireccionRequestDTO.java     <- calle, numero, pisoDepto, codigoPostal, localidad
│   │   ├── LoginRequestDTO.java
│   │   ├── ProductoRequestDTO.java
│   │   ├── ImagenProductoRequestDTO.java <- url (post-Cloudinary), orden, esPrincipal
│   │   ├── CategoriaRequestDTO.java
│   │   ├── TagRequestDTO.java
│   │   ├── ItemCarritoRequestDTO.java
│   │   ├── PedidoRequestDTO.java
│   │   ├── RechazoPedidoRequestDTO.java
│   │   ├── AprobacionComercioRequestDTO.java
│   │   ├── HorarioRequestDTO.java        <- diaSemana, horaApertura, horaCierre (Fase 16a)
│   │   └── ClienteEditarPerfilRequestDTO.java <- nombre, apellido, telefono (Fase 16a)
│   └── response/
│       ├── ApiResponse.java             <- envoltorio estándar {mensaje, data}
│       ├── UsuarioResponseDTO.java
│       ├── DireccionResponseDTO.java
│       ├── ComercioResponseDTO.java     <- self-service (ComercioController) exclusivamente; suma `representante` (RepresentanteResponseDTO anidado, puede ser null) desde la corrección retroactiva — nunca usado por CatalogoController, que usa ComercioPublicoResponseDTO
│       ├── ProductoResponseDTO.java
│       ├── ImagenProductoResponseDTO.java
│       ├── CategoriaResponseDTO.java     <- suma `cantidadProductos` (Fase 16, Tramo 9) — conteo real vía `ProductoRepository.countByCategoriaId`, para AD15
│       ├── TagResponseDTO.java           <- suma `cantidadProductos` (Fase 16, Tramo 9) — conteo real vía `ProductoTagRepository.countByTagId`, para AD20
│       ├── CarritoResponseDTO.java
│       ├── PedidoResponseDTO.java       <- suma `nombreCliente` (Tramo 16.5, dashboard de Comercio; sin catálogo público de clientes para resolverlo del lado del frontend)
│       ├── NotificacionResponseDTO.java
│       ├── ProvinciaResponseDTO.java
│       ├── LocalidadResponseDTO.java
│       ├── CloudinarySignatureResponseDTO.java <- firma de subida para el frontend
│       ├── HorarioResponseDTO.java       <- id, diaSemana, horaApertura, horaCierre (Fase 16a)
│       ├── ClienteResponseDTO.java       <- perfil propio del Cliente (Fase 16a); suma `direccion` (Tramo 16.3, DireccionResponseDTO anidada, solo lectura)
│       ├── ResumenPedidosHoyResponseDTO.java <- totalFacturadoHoy, cantidadPedidosHoy, cantidadPendientes (Tramo 16.5, dashboard de Comercio)
│       ├── ComercioAdminResponseDTO.java <- variante admin-only de ComercioResponseDTO para AdministradorController (Tramo 16.8, renombrada desde ComercioPendienteResponseDTO en el Tramo 16.21 al dejar de ser exclusiva de pendientes): suma fechaRegistro, condicionIva, emailCuenta (login), fotoPerfilUrl (Tramo 16.21) y `representante` (corrección retroactiva, ver docs/DECISIONES.md) — usada tanto por `listarComerciosPendientes` como por `listarComerciosAprobados`
│       ├── ClienteAdminResponseDTO.java <- listado de solo lectura de Clientes para AdministradorController (Tramo 16.21): id, nombre, apellido, dni, email, estado, fechaRegistro
│       ├── AdministradorResponseDTO.java <- perfil propio del Administrador logueado (Tramo 16.21, solo nombre/apellido, para el header del dashboard)
│       ├── MetricasAdminResponseDTO.java <- comerciosPendientes, comerciosTotal, clientesTotal, categoriasActivas, tagsActivos (Tramo 16.8, dashboard de Administrador)
│       ├── ComercioPublicoResponseDTO.java <- variante pública de ComercioResponseDTO para CatalogoController (sin autenticación): mismos campos que ComercioResponseDTO tenía antes de la corrección retroactiva, sin `representante` — el DNI del representante nunca debe viajar sin autenticación (ver docs/DECISIONES.md)
│       └── RepresentanteResponseDTO.java <- nombre, apellido, dni, telefono, fechaNacimiento; anidado en ComercioResponseDTO y ComercioAdminResponseDTO, nunca en ComercioPublicoResponseDTO
├── entities/
│   ├── Provincia.java
│   ├── Localidad.java
│   ├── Usuario.java
│   ├── PersonaFisica.java
│   ├── PersonaJuridica.java
│   ├── Cliente.java
│   ├── Comercio.java
│   ├── Administrador.java
│   ├── Direccion.java
│   ├── Token.java
│   ├── Categoria.java
│   ├── Tag.java
│   ├── Producto.java
│   ├── ImagenProducto.java
│   ├── ProductoTag.java
│   ├── Carrito.java
│   ├── ItemCarrito.java
│   ├── Pedido.java
│   ├── DetallePedido.java
│   ├── Notificacion.java
│   ├── Sesion.java                       <- agregada en la Fase 7 (ver docs/DECISIONES.md)
│   ├── HistorialEstadoComercio.java      <- agregada en la Fase 8 (ver docs/DECISIONES.md)
│   └── Horario.java                      <- agregada en la Fase 16a (ver docs/DECISIONES.md)
├── repositories/         <- un XxxRepository por cada entidad de arriba (JpaRepository<Entidad, PK>)
├── services/
│   ├── AuthService.java
│   ├── RegistroService.java
│   ├── GeografiaService.java            <- listar provincias/localidades para los selectores
│   ├── ComercioService.java
│   ├── AdministradorService.java
│   ├── CategoriaService.java
│   ├── TagService.java
│   ├── ProductoService.java
│   ├── CatalogoService.java             <- delgado, delega en ComercioService/ProductoService
│   ├── CarritoService.java
│   ├── PedidoService.java
│   ├── NotificacionService.java
│   ├── EmailService.java
│   ├── CloudinaryService.java           <- generar firma de subida, validar límite de imágenes
│   ├── ClienteService.java              <- autoservicio de perfil propio del Cliente (Fase 16a)
│   └── TestSupportService.java          <- exclusivo del perfil `test` (Fase 14), atajo de token sin email real
├── controllers/
│   ├── HealthController.java            <- GET /api/v1/health, implementado en la Fase 3
│   ├── AuthController.java
│   ├── ComercioController.java
│   ├── AdministradorController.java
│   ├── CategoriaController.java
│   ├── TagController.java
│   ├── ProductoController.java
│   ├── CatalogoController.java          <- endpoints públicos de exploración
│   ├── GeografiaController.java         <- GET /provincias, GET /localidades?provinciaId=
│   ├── CarritoController.java
│   ├── PedidoController.java
│   ├── NotificacionController.java
│   ├── ClienteController.java           <- GET/PUT /api/v1/clientes/perfil (Fase 16a)
│   └── TestController.java              <- exclusivo del perfil `test` (Fase 14), GET /test/token(-verificacion)
├── exceptions/
│   ├── GlobalExceptionHandler.java      <- @RestControllerAdvice
│   ├── RecursoNoEncontradoException.java
│   ├── ConflictoDeNegocioException.java
│   └── ValidacionException.java
└── mappers/              <- opcional; MVP usa mapeo manual en el Service, no MapStruct
```

**Separación de responsabilidades:** la lógica de negocio vive en `services/`; `repositories/` queda "tonto" (solo queries derivadas o `@Query`); `controllers/` queda aún más tonto (solo mapear HTTP ↔ DTO y delegar al service). Mapeo Entity↔DTO manual dentro del Service, no MapStruct — más explícito para este proyecto.

**Ajustes hechos durante la Fase 3** respecto al árbol original: `enums/` se implementó como paquete hermano de `entities/` (no `entities/enums/`), porque los enums no dependen de que las Entities existan y así quedan disponibles antes de la Fase 4. Se agregó además el paquete `validation/` (`annotations/` + `validators/`), no previsto en la estructura original de la guía, para las anotaciones de Bean Validation custom (ej. `@ValidarCuit`) que se van a usar en los DTOs de request a partir de la Fase 5 — nunca en las Entities de JPA.

Convenciones de nombres: Entities en singular (`Producto`), DTOs `<Entidad>RequestDTO` / `<Entidad>ResponseDTO`, Controllers con `@RequestMapping("/api/v1/<recurso-en-plural>")`.

### 3.1 Convención de respuesta HTTP estándar (Fase 3.3 de la guía)

```java
public class ApiResponse<T> {
    private String mensaje;
    private T data;
    // constructores, getters
}
```

Todo controller devuelve `ResponseEntity<ApiResponse<XxxResponseDTO>>` (o `ApiResponse<List<XxxResponseDTO>>` para listados). Nunca se expone la entidad, siempre DTO envuelto, y siempre hay `mensaje`.

---

## 4. Reglas transversales no negociables

Estas reglas se aplican siempre, sin excepción. Varias se refuerzan con hooks (ver `.claude/hooks/`, Anexo C de la guía) precisamente para no depender de que se recuerden en cada prompt.

1. **`List<T>` como colección por defecto** en toda la capa de servicios y repositorios. `Set<T>` solo con razón concreta (ej. evitar duplicados en `ProductoTag`), `Map` solo cuando el acceso es por clave. Cualquier excepción se justifica con un comentario breve en el código.
2. **Los controllers nunca devuelven una entidad JPA directamente.** Siempre un DTO de respuesta (`XxxResponseDTO`) envuelto en `ResponseEntity<ApiResponse<...>>`.
3. **Toda respuesta HTTP de mutación** (POST/PUT/PATCH/DELETE) devuelve un cuerpo con al menos un campo `mensaje` (clase `ApiResponse<T>` estándar), además del recurso si corresponde.
4. **Códigos de estado HTTP explícitos y semánticos**, nunca `200` por defecto para todo:
   - `201 Created` al crear.
   - `200 OK` para lecturas y actualizaciones.
   - `204 No Content` para eliminaciones sin cuerpo (en este proyecto se prioriza devolver `mensaje`, ver Fase 9 de la guía).
   - `400 Bad Request` para validaciones de entrada fallidas.
   - `401 Unauthorized` para token ausente/inválido.
   - `403 Forbidden` para rol sin permiso.
   - `404 Not Found` para recurso inexistente.
   - `409 Conflict` para violaciones de estado/regla de negocio (email duplicado, producto de otro comercio en el carrito, límite de imágenes superado, etc.).
5. **Nunca hardcodear el puerto** en ningún lado más que `application.properties`.
6. **Nunca commitear credenciales** (contraseña de MySQL, SMTP, `jwt.secret`, credenciales de Cloudinary) — siempre vía variables de entorno o `application-local.properties` ignorado por git.
7. **Convenciones Lombok en toda Entity JPA** (`entities/`, Fase 4 en adelante):
   - `@Getter` a nivel de clase.
   - `@Setter` **campo por campo, nunca a nivel de clase** (evita `@Data`, que generaría `equals`/`hashCode` sobre todos los campos con riesgo de recursión infinita en relaciones bidireccionales).
   - **El campo `id` nunca lleva `@Setter`, sin excepción**, ni en entidades con id autogenerado (`@GeneratedValue`) ni en las de id externo (ej. `Provincia`/`Localidad`, cargadas por el ETL de Fase 2bis). El resto de los campos sí llevan `@Setter` individual.
   - `@EqualsAndHashCode(of = "id")` explícito en toda entidad.
   - Esta regla aplica a **toda** entidad del proyecto, pasada, presente y futura — no es un caso puntual de ninguna entidad en particular.
   - **`ProductoTag` es la única entidad del proyecto con clave compuesta** (`@EmbeddedId` sobre `ProductoTagId`, con `@MapsId("productoId")`/`@MapsId("tagId")` en sus dos `@ManyToOne`). No es una inconsistencia con el resto de las entidades (todas con PK escalar `Integer`/`String`) — es el mapeo estándar de JPA para una tabla de unión N:M con atributos propios de la relación (`producto_tag`, PK compuesta `(producto_id, tag_id)`). `ProductoTagId` es un `@Embeddable`, no una `@Entity`: no tiene tabla propia, y como no tiene un campo literal `id`, sus dos campos (`productoId`, `tagId`) sí llevan `@Setter` individual sin violar la regla de "el campo `id` nunca tiene `@Setter`".
8. **Cero comentarios dentro de `entities/`**, sin excepción: nada de Javadoc de clase, nada de comentarios inline, nada de comentarios de campo. Las clases de `entities/` quedan autoexplicativas por nombre de clase, nombre de campo y anotaciones únicamente. Cualquier explicación de diseño (por qué un id no es autogenerado, por qué un campo no tiene setter, notas de alcance del modelo, etc.) vive en `CLAUDE.md` o en `docs/modelo-mvp.md`, nunca en el código de la entidad. Esta regla es exclusiva del paquete `entities/` — el resto del proyecto (`repositories/`, `services/`, `controllers/`, `dto/`, etc.) sigue documentándose con normalidad donde corresponda.
9. **Ninguna fase se da por cerrada, ni se avanza a la siguiente, sin que todos los sub-puntos de su sección en `GUIA-IMPLEMENTACION-MVP-BAJONEA.pdf` tengan evidencia real documentada en `docs/DECISIONES.md`.** "Evidencia real" significa: para un punto de comportamiento (una regla de negocio, una validación, un flujo completo), una prueba end-to-end contra la base real (`curl` + verificación de la fila resultante cuando aplica) — `./mvnw compile`/`BUILD SUCCESS` alcanza únicamente para puntos de sintaxis (que el código compile), nunca como sustituto de probar que el comportamiento es correcto. Si un sub-punto queda fuera de una fase a propósito (porque depende de infraestructura de una fase posterior, ej. Cloudinary real), tiene que quedar diferido **explícitamente**, con destino a la fase que lo va a cubrir — mismo formato ya usado para `LoginRequestDTO`/`RechazoPedidoRequestDTO`/`AprobacionComercioRequestDTO`/`ImagenProductoRequestDTO`/`CloudinarySignatureResponseDTO` (`docs/DECISIONES.md`, entrada "DTOs de auth y pedido/comercio diferidos..."). Nunca queda como omisión silenciosa — ni un sub-punto sin mencionar, ni un "ya probado" sin la prueba real detrás. Antes de cerrar una fase o dar por buena la anterior al pasar a la siguiente, repasar el checklist real de esa sección de la guía punto por punto, no solo lo que ya quedó escrito en `docs/DECISIONES.md` de sesiones previas.
10. **Un commit de Git por cada cierre formal de fase.** El repo vive en la raíz real del proyecto (`Bajoneá/`, no `03. Implementación/` — ver `git remote -v` para confirmar el remoto configurado), con un `.gitignore` de raíz que complementa a `backend/.gitignore` sin duplicar sus reglas (ver el propio `.gitignore` de raíz para el detalle). Cada vez que una fase pasa de 🔄/⬜ a ✅ en la tabla de §6 (incluida una reapertura puntual que vuelve a cerrar una fase ya cerrada, ej. Fase 8 el 2026-07-18), corresponde un commit local con un mensaje descriptivo del estado alcanzado — nunca se agrupan varias fases en un solo commit salvo que ya vinieran así de antes de esta regla (el commit inicial del 2026-07-18 agrupó las Fases 1-9 precisamente porque no existía ningún commit previo). Antes de cada commit: revisar `git status` para no arrastrar cambios fuera del alcance de la fase que se está cerrando (ver `docs/DECISIONES.md`, entrada del 2026-07-18 "Primer commit versionado del proyecto" — un archivo modificado de `01. Análisis de Requerimientos` quedó deliberadamente fuera del primer commit por este mismo criterio), y confirmar que ningún archivo con credenciales reales (`application-local.properties`, `.env`, o cualquier valor real de SMTP/Cloudinary/`JWT_SECRET`/password de MySQL) esté en el diff staged. Push a GitHub queda fuera de esta regla — es una decisión aparte que toma el usuario explícitamente, no automática por cierre de fase.
11. **Ningún archivo `.html`, `.css` o `.js` generado en `frontend/` debe contener comentarios de ningún tipo (ni de encabezado, ni explicativos, ni TODO, ni código comentado), bajo ninguna circunstancia.** Esto aplica a todos los tramos de Fase 16 sin excepción.
12. **Al usar el navegador o el MCP contra el archivo de Figma, la ÚNICA página válida como referencia es "MVP".** Ninguna otra página del archivo (ej. "Admin, Global & Auth") debe usarse para extraer nombres de capas, contenido, ni capturas — pertenecen al proyecto completo, fuera del alcance del MVP cerrado en Fase 15.
13. **Al usar el navegador contra el archivo de Figma, está terminantemente prohibido editar, mover, borrar, o alterar cualquier elemento de cualquier pantalla, bajo cualquier circunstancia.** El único uso permitido del navegador contra Figma es navegación de solo lectura (ubicar pantallas) y exportación de capturas PNG vía el panel "Exportar" → "Vista previa". Si accidentalmente se modifica algo, se debe deshacer inmediatamente (Ctrl+Z) antes de continuar con cualquier otra acción, y avisar al dueño del proyecto explícitamente en la respuesta.

---

## 5. Las 20 entidades del modelo MVP original (histórico — modelo vigente en `docs/diccionario-de-datos.md`)

> Esta sección describe las entidades ya implementadas en código (Fases 4-16a), sin cambios por la enmienda de §1bis — siguen siendo una descripción correcta de lo que hay hoy en `entities/`. Lo que cambia es que ya **no** son "todas las entidades del proyecto": el modelo vigente completo (41 tablas) vive en `docs/diccionario-de-datos.md` y `docs/bajonea_final.sql`, detalle de entidades nuevas pendientes de implementar en `docs/AUDITORIA-MODELO-DATOS-NUEVO.md` §1.3.

Detalle completo de columnas y relaciones del modelo MVP original en `docs/modelo-mvp.md`. Acá solo el rol de cada una:

1. **Provincia** — catálogo estático de provincias argentinas, precargado por ETL desde Georef (Fase 2bis).
2. **Localidad** — catálogo estático de localidades, FK a Provincia, precargado por el mismo ETL.
3. **Usuario** — identidad base (email, password hash, rol, estado); Cliente/Comercio/Administrador cuelgan de acá.
4. **PersonaFisica** — datos de persona física (nombre, apellido, DNI); subtipo de Usuario vía `@MapsId`.
5. **PersonaJuridica** — datos de persona jurídica (razón social, CUIT); subtipo de Usuario vía `@MapsId`.
6. **Cliente** — rol de comprador, extiende PersonaFisica, tiene direcciones y carrito. Autoservicio de perfil propio desde la Fase 16a (`GET`/`PUT /api/v1/clientes/perfil`, edita solo `nombre`/`apellido`/`telefono`; sin endpoint de direcciones ni de baja de cuenta).
7. **Comercio** — rol de vendedor, extiende PersonaJuridica, tiene estado de aprobación y productos.
8. **Administrador** — rol de gestión interna, extiende PersonaFisica, sin registro público (se siembra por Flyway).
9. **Direccion** — modelo completo (calle, número, piso/depto, código postal, localidad), FK mutuamente excluyente a Cliente o Comercio.
10. **Token** — token de un solo uso; tipos `VERIFICACION_EMAIL`, `RECUPERACION_PASSWORD`, `REACTIVACION_CUENTA` (los 2 últimos sumados en la Fase 7, ver `docs/DECISIONES.md`).
11. **Categoria** — clasificación de productos, gestionada por el Administrador.
12. **Tag** — etiqueta libre de productos, gestionada por el Administrador.
13. **Producto** — ítem del catálogo de un Comercio, con precio y estado (disponible/agotado/descontinuado).
14. **ImagenProducto** — una fila por imagen de la galería de un Producto (hasta 5, una marcada como principal), URL provista por Cloudinary.
15. **ProductoTag** — tabla de relación N:M entre Producto y Tag.
16. **Carrito** — un registro por Cliente, atado a un único Comercio a la vez.
17. **ItemCarrito** — línea de producto + cantidad dentro de un Carrito.
18. **Pedido** — pedido confirmado por un Cliente a un Comercio, estado recortado a `PENDIENTE → ACEPTADO/RECHAZADO`.
19. **DetallePedido** — línea de producto + cantidad + precio snapshot (inmutable) dentro de un Pedido.
20. **Notificacion** — mensaje in-app para un usuario (leída/no leída), consumido por polling.
21. **Sesion** — historial de sesiones de un usuario; `activa=true` identifica la sesión vigente, consultada por `JwtAuthenticationFilter` en cada request. Reincorporada en la Fase 7 mediante enmienda formal de alcance (ver `docs/DECISIONES.md` y `docs/modelo-mvp.md` nota de alcance 12) — habilita invalidar sesiones de forma real en bloqueo, recuperación de contraseña y login concurrente, algo que un JWT puramente stateless no puede cumplir.
22. **HistorialEstadoComercio** — registro histórico append-only de las transiciones de `Comercio.estado`, con motivo obligatorio en rechazo. Reincorporada en la Fase 8 con el mismo criterio que `Sesion` (ver `docs/DECISIONES.md` y `docs/modelo-mvp.md` nota de alcance 13) — única fuente fiel al modelo para el motivo de rechazo, ya que el diccionario completo eliminó `Comercio.motivo_rechazo` en v1.1 a favor de esta tabla.
23. **Horario** — franja horaria de atención de un Comercio (`diaSemana`, `horaApertura`, `horaCierre`); 0 o más filas por día (un día sin ninguna fila significa cerrado ese día, sin flag booleano dedicado; horario partido = 2+ filas del mismo día). Reincorporada en la Fase 16a con el mismo criterio que `Sesion`/`HistorialEstadoComercio` (ver `docs/DECISIONES.md`, 2026-07-20) — alcance acotado a carga en el registro de Comercio + lectura, sin endpoint de edición todavía (pospuesto a una fase futura).

**Entidades que en el modelo MVP original no entraban** (histórico — desde la enmienda de §1bis, todas están en alcance, pendientes de implementación): `HistorialEstadoUsuario`, `HistorialEstadoPedido`, `Soporte`, `Reclamo`, `ConfiguracionTarifa`, `CuentaMercadoPago`, `Pago`, `NotaCredito` — más `Dueño`, `Empleado`, `EmpleadoComercio`, `RedSocial`, `GrupoExtra`, `Extra`, `ProductoGrupoExtra`, `ItemCarritoExtra`, `DetallePedidoExtra` (no nombradas en la lista original porque el modelo completo de entonces no las tenía como concepto separado). Ver §1bis.

---

## 5bis. Validaciones (Fase 5 en adelante)

Todas viven en `validation/` (`validation/annotations/` + `validation/validators/`), hermano de `entities/` (ver §3). Se aplican **solo en `dto/request/`**, nunca en las Entities. Detalle completo, con guía de dónde aplicar cada una, en [.claude/skills/skill-validaciones/SKILL.md](.claude/skills/skill-validaciones/SKILL.md) — acá solo el catálogo y la regla general.

**Regla general:** usar `jakarta.validation` estándar (`@NotBlank`, `@NotNull`, `@Email`, `@Size`, `@Pattern`, `@Positive`, `@PositiveOrZero`, `@Past`, `@PastOrPresent`, `@Future`, `@Min`, `@Max`, `@Digits`) directo en el campo siempre que alcance. Crear/usar una anotación custom **únicamente** cuando hace falta lógica real que un `@Pattern` no resuelve (dígito verificador, normalización, coherencia entre campos, regla de negocio).

**Las 9 anotaciones custom del catálogo** (target `FIELD` salvo la última, que es `TYPE`):

1. `@ValidarCuit` — CUIT, 11 dígitos + dígito verificador módulo 11 (AFIP).
2. `@ValidarDni` — DNI, 7-8 dígitos, rango [1.000.000, 99.999.999].
3. `@ValidarTelefonoArgentino` — teléfono argentino, tolerante a +54/9/0/15.
4. `@ValidarNombrePropio` — nombre/apellido, solo letras Unicode + espacios + guiones.
5. `@ValidarPasswordSegura` — mínimo 8 caracteres, 1 mayúscula, 1 número (sin símbolo).
6. `@ValidarCodigoPostalArgentino` — 4 dígitos clásico o CPA alfanumérico de 8.
7. `@MayorDeEdad` — sobre `LocalDate`, 18 años o más.
8. `@ValidarUrlCloudinary` — esquema `https` + host `res.cloudinary.com`.
9. `@DireccionExclusionMutua` — nivel de clase, exactamente uno de `clienteId`/`comercioId`.

**Aplicado en Fase 5** (tanda "cadena de identidad", ver `docs/DECISIONES.md`): los campos de `PersonaFisica` + `Usuario` no tienen DTO propio — se plegaron en `RegistroClienteRequestDTO` (`dto/request/`), el body real de `POST /api/v1/auth/registro/cliente`, con `@ValidarNombrePropio` (`nombre`, `apellido`), `@ValidarDni` (`dni`), `@Past` + `@MayorDeEdad` (`fechaNacimiento`), `@ValidarTelefonoArgentino` (`telefono`), `@Email` + `@NotBlank` (`email`), `@ValidarPasswordSegura` (`password`). Todavía sin el campo `direccion` (se agrega cuando exista `DireccionRequestDTO`). `UsuarioResponseDTO` (`id`, `email`, `rol`, `estado`, sin password) ya generado.

**Aplicado en Fase 5** (tanda de Comercio): `PersonaJuridica` tampoco tiene DTO propio — sus campos se plegaron en `RegistroComercioRequestDTO` (`dto/request/`), el body real de `POST /api/v1/auth/registro/comercio`, con `@ValidarCuit` (`cuit`), `@NotBlank` + `@Size` (`razonSocial`, `domicilioFiscal`, `nombre`), `@PastOrPresent` (`fechaInicioActividades`), `@ValidarTelefonoArgentino` (`telefono`), `@ValidarPasswordSegura` (`password`), `@Email` (`email` de login y `emailContacto` público del comercio, a propósito distintos), `@Valid` anidado sobre `DireccionRequestDTO`. **Ampliado retroactivamente** (corrección a los Tramos 16.1/16.8, ver `docs/DECISIONES.md`, 2026-07-22): igual que `PersonaFisica` tampoco tiene DTO propio, sus campos del representante del comercio se plegaron en el mismo `RegistroComercioRequestDTO`, con las mismas anotaciones que ya usa `RegistroClienteRequestDTO` — `@ValidarNombrePropio` (`nombreRepresentante`, `apellidoRepresentante`), `@ValidarDni` (`dniRepresentante`), `@ValidarTelefonoArgentino` (`telefonoRepresentante`), `@Past` + `@MayorDeEdad` (`fechaNacimientoRepresentante`).

**Aplicado en Fase 11** (galería de producto + foto de perfil de comercio, vía Cloudinary): `ImagenProductoRequestDTO` (`url` con `@NotBlank` + `@ValidarUrlCloudinary`, `orden` con `@NotNull` + `@PositiveOrZero`, `esPrincipal` sin validación por ser `boolean` primitivo) y `FotoPerfilComercioRequestDTO` (`url` con `@NotBlank` + `@ValidarUrlCloudinary`), ambos DTOs nuevos de esta fase. `@ValidarUrlCloudinary` sigue validando solo dominio, no propiedad — el gap de suplantación queda mitigado por el propio flujo de firma (`CloudinaryService`, scoped por `comercioId`/`productoId` resueltos desde el JWT), no por la anotación; detalle completo en [.claude/skills/skill-validaciones/SKILL.md](.claude/skills/skill-validaciones/SKILL.md).

---

## 6. Índice de fases (dónde estamos, qué sigue)

Secuencia completa definida en `GUIA-IMPLEMENTACION-MVP-BAJONEA.md`. Estado actual: **Fase 15 cerrada; Fase 16a cerrada (ClienteController + Horario); Fase 16 — los 9 tramos de contenido están completos (base + Auth/Registro; Catálogo/Comercio/Perfil de Cliente; Carrito y Checkout; Pedidos y Notificaciones; Comercio — dashboard y perfil; Comercio — CRUD de productos; Comercio — pedidos recibidos; Administrador — aprobación de comercios; Administrador — categorías y tags), pero la fase queda 🔄 en curso hasta que el dueño del proyecto confirme el checklist GLOBAL de cierre (pantallas del catálogo de Fase 15, consumo real de backend, Cloudinary end-to-end, selector geográfico, flujo completo sin errores de consola, cero comentarios) — ver `docs/DECISIONES.md`, entrada de cierre global de esta misma fecha. 2 gaps reales detectados en esa verificación, no cerrados en este tramo: pantallas `C42` y `CO27` (confirmadas IN en `docs/PANTALLAS-MVP-FASE15.md` §1) sin archivo construido ni exclusión documentada — pendientes de decisión del usuario antes de poder cerrar la fase.**

| Fase | Nombre | Estado |
|---|---|---|
| 0 | Preparación del entorno de trabajo con Claude Code | ✅ cerrada |
| 1 | Inicialización del proyecto Spring Boot | ✅ cerrada |
| 2 | Base de datos: modelo MVP, Flyway y phpMyAdmin | ✅ cerrada |
| 2bis | Carga del catálogo geográfico (ETL Georef) | ✅ cerrada |
| 3 | Estructura de capas y convenciones de código | ✅ cerrada |
| 4 | Entities (JPA) | ✅ cerrada |
| 5 | DTOs y mapeo | ✅ cerrada |
| 6 | Repositories | ✅ cerrada |
| 7 | Configuración: seguridad, JWT y autorización por rol | ✅ cerrada — checklist en §6.7bis. Alcance ampliado (ver `docs/DECISIONES.md`, 2026-07-17): reincorpora `Sesion`, agrega recuperación de contraseña, bloqueo de cuenta y reactivación de cuenta. |
| 8 | Services (lógica de negocio) | ✅ cerrada — `RegistroService` (8.1), `GeografiaService` (8.1bis), `AdministradorService` (8.3, aprobación de comercios + `gestionarCategoria`/`gestionarTag` vía `CategoriaService`/`TagService` separados), `ProductoService`/`ComercioService` (8.4; `editarPerfil` + `verPerfil` — este último reabierto y cerrado el 2026-07-18 tras el inventario de Fase 9, ver `docs/DECISIONES.md`; gestión de galería `ImagenProducto` diferida explícitamente a Fase 11), `CarritoService` (8.5), `PedidoService` (8.6) y `NotificacionService` (8.7, incluye `contarNoLeidas` para el badge de polling) implementados y probados end-to-end contra la base real, todos los sub-puntos de 8.1 a 8.8 con evidencia documentada (ver `docs/DECISIONES.md`, 2026-07-17 y 2026-07-18 — regla transversal 9 de §4). Bug real encontrado y corregido probando `GeografiaService`: excepción no cubierta en un endpoint público se enmascaraba como `401` en vez de su status real (`SecurityConfig`/`GlobalExceptionHandler`, ver misma entrada). Ver `docs/CONCURRENCIA-Y-TRANSACCIONES.md` (aprobado 2026-07-17) para las decisiones de concurrencia por módulo, y las skills actualizadas en `.claude/skills/` para los patrones recurrentes ya establecidos. |
| 9 | Controllers y contrato de respuestas HTTP | ✅ cerrada — inventario completo del 2026-07-18 contra 9.3 (ver `docs/DECISIONES.md`): `ComercioController` completado (reapertura puntual de Fase 8); 3 deviaciones de path/verbo HTTP (`AdministradorController`/`PedidoController`/`NotificacionController`) confirmadas como decisión de diseño (§7bis); `CatalogoController`/`CatalogoService` implementados y probados end-to-end. Checklist formal de cierre de la guía (3 puntos) corrido contra los 11 Controllers en una sola entrada: único hueco real es la galería de `ProductoController`, diferida a Fase 11; 2 casos de status HTTP sin evidencia previa (`POST /auth/cambiar-password`, regla `hasRole("CLIENTE")`) probados ahora sin sorpresas; los 12 Controllers actuales pasan el hook `bloquear-entity-en-controller.js` real (cobertura histórica de los 9 escritos en sesiones previas no verificable — sin git history en el proyecto — dejado explícito, no asumido). |
| 10 | Email real para verificación de cuenta | ✅ cerrada (reabierta y vuelta a cerrar el 2026-07-31) — cierre original (2026-07-20, texto histórico sin tocar): Brevo/SMTP bloqueado permanentemente (`502 SMTP account is not yet activated`, sin resolución posible tras contactar soporte); migrado a Resend (API REST, `com.resend:resend-java`). `EmailService` reescrito sobre el cliente de Resend (misma firma pública, `MailConfig` ahora expone el bean `Resend` igual que `CloudinaryConfig`), `spring-boot-starter-mail`/`spring.mail.*` eliminados por completo (no comentados). Probado de punta a punta con email real (`bukle.arg@gmail.com`): Resend devolvió un id real de envío, el usuario confirmó recepción (remitente `info@bajonea.ar`, SPF/DKIM ok), verificación con el token real del email → `usuario.estado` `PENDIENTE → ACTIVO`, login posterior `200`. Regresión Postman completa (perfil `test`): 49/49 requests, 113/113 assertions, 0 fallos. Mismo endpoint validado también desde Swagger UI (`Try it out` real, no simulado) con transición `PENDIENTE → ACTIVO` confirmada en base. **Reapertura del 2026-07-31** (`docs/AUDITORIA-POSTMAN-FASE10-14.md`): esa prueba de email real había quedado atada al mecanismo viejo de verificación por link/UUID; desde el Tramo 16.11/16.12 el mecanismo primario es un código de 6 dígitos. Cerrado a nivel endpoint el mismo día (`POST /auth/verificar` con código real, no bypass — `docs/DECISIONES.md`, 2026-07-31, Paso 5) y **confirmado por Diego el 2026-07-31 vía uso real repetido** (recibe el mail y el código correcto de forma consistente) — sin necesidad de una prueba manual adicional. Ver `docs/DECISIONES.md`, entrada de cierre del 2026-07-31. |
| 11 | Cloudinary: subida firmada de imágenes de producto | ✅ cerrada — galería de `ProductoController` (los 3 endpoints diferidos de Fase 9) + `POST /productos/{id}/cloudinary/firma` + foto de perfil de `Comercio` (`POST /comercios/perfil/foto/firma` + `PUT /comercios/perfil/foto`, ver §7bis). Credenciales de una cuenta de Cloudinary de prueba, cuenta definitiva pendiente de `info@bajonea.ar` — ver `docs/DECISIONES.md`, 2026-07-18. |
| 12 | Notificaciones in-app (polling) | ✅ cerrada — mayormente construida por adelantado en Fases 8/9 (`Notificacion`/`NotificacionRepository`/`NotificacionService`/`NotificacionController`, 5 sitios de `notificacionService.crear` en `PedidoService`/`ProductoService`/`AdministradorService`). Esta fase fue inventario + evidencia faltante, no código nuevo: los 3 sitios de `PedidoService` (nuevo pedido, aceptado, rechazado) solo tenían prueba **anterior** a la centralización del 2026-07-18 — probados ahora contra el código post-refactor. Ver `docs/DECISIONES.md`, 2026-07-19. |
| 13 | Documentación automática (springdoc-openapi) | ✅ cerrada — `OpenApiConfig` (bean `OpenAPI`, esquema `bearerAuth` HTTP/Bearer/JWT). Bug real encontrado y corregido en `SecurityConfig`: `/swagger-ui.html` (el link literal de la guía) no estaba cubierto por `/swagger-ui/**` — es un redirect propio de springdoc, no un subpath — y daba `401`; agregado como entrada propia. 43 endpoints confirmados en el spec (incluida la galería/foto de perfil de Fase 11), sin campos sensibles en los 58 schemas generados. Flujo "Authorize" probado de punta a punta vía la UI real (no solo `curl`) para los 3 roles. Ver `docs/DECISIONES.md`, 2026-07-19. |
| 14 | Testing de API con Postman | ✅ cerrada (reabierta y vuelta a cerrar el 2026-07-31) — cierre original (2026-07-19, texto histórico sin tocar): colección de 49 requests (9 carpetas) armada vía MCP de Postman en workspace dedicado (`Bajoneá MVP`), corrida completa con Newman (49/49 requests, 113/113 assertions, 0 fallos) contra el backend real; exportada a `postman/`, fuente de verdad versionada. `TestController`/`TestSupportService` (`@Profile("test")`) generalizados a `GET /api/v1/test/token?email=&tipo=` para cubrir también recuperación de contraseña, no solo verificación de cuenta — queda permanente bajo el perfil `test`, no es un atajo con fecha de vencimiento. **Limitación confirmada del MCP de Postman** (no del proyecto): la copia alojada en la nube tiene los 49 requests completos (método/URL/headers/body, re-verificado con `getCollection`) pero sin los scripts de la pestaña "Tests" — `putCollection` los descarta en silencio en items anidados, `updateCollectionRequest` da `404` en cualquier item dentro de una carpeta, y el servidor MCP no expone ningún tool de borrado para reconstruir sin duplicar. Decisión aceptada entonces: no forzar más escritura vía API — sigue vigente, la reapertura tampoco tocó la nube. Ver `docs/DECISIONES.md`, 2026-07-19 (entrada original + entrada de re-verificación). **Reapertura del 2026-07-31** (`docs/AUDITORIA-POSTMAN-FASE10-14.md`): la colección había quedado desactualizada (endpoints agregados después de Fase 14 sin cobertura) y además **rota** contra el backend actual (`RegistroComercioRequestDTO` exige 5 campos de representante desde el 2026-07-22, ausentes en el body de Comercio A/B). Reparada y ampliada el mismo día: 203 requests (20 carpetas), 416/416 assertions en verde contra el backend real, cubriendo los 69 endpoints reales del backend (no los 43 de Fase 13). **Cierre confirmado por Diego el 2026-07-31** tras revisar el checklist completo — los 4 puntos que quedaron deliberadamente fuera de esta ronda (estados `INACTIVO`/`SUSPENDIDO` sin vía real de API, expiración real de tokens, deadlock de reordenamiento concurrente de imágenes, `@DireccionExclusionMutua` sin forma real de ejercitarla) confirmados como fuera de alcance del MVP o limitación conocida documentada, no como deuda pendiente. Ver `docs/DECISIONES.md`, entradas del 2026-07-31 (relevamiento, reparación/ampliación, y cierre). |
| 15 | Figma: checklist de pantallas MVP | ✅ cerrada — catálogo cerrado en [docs/PANTALLAS-MVP-FASE15.md](docs/PANTALLAS-MVP-FASE15.md): 87 pantallas confirmadas IN de las ~150 del proyecto completo (+ 1 pantalla nueva a diseñar en Fase 16, `CO33` — dashboard de Comercio aprobado/operando sin MercadoPago, sin equivalente en el listado original), 66 OUT categorizadas (exclusión de alcance §1, sin mecanismo real verificado en código, búsqueda/filtro global inexistente, o sin backend construido — 4 categorías distintas, no todas por la misma razón). 4 puntos ambiguos resueltos con el usuario antes de cerrar (direcciones de cliente, búsqueda/filtros, dashboard de Comercio, pantallas sin backend). Sin tocar Figma — el usuario arma la página nueva a mano copiando las capas del checklist. |
| 16a | ClienteController + reincorporación de Horario | ✅ cerrada — dos ampliaciones de alcance confirmadas por el dueño del proyecto, mismo criterio que `Sesion`/`HistorialEstadoComercio`: (1) `ClienteController`/`ClienteService` (`GET`/`PUT /api/v1/clientes/perfil`, rol CLIENTE), autoservicio de perfil del propio Cliente — edita solo `nombre`/`apellido`/`telefono`, `email`/`dni`/`fechaNacimiento` quedan de solo lectura; sin endpoint de direcciones ni de baja de cuenta, confirmado fuera de alcance. (2) `Horario` (entidad #23, ver §5) — persistida en el registro de Comercio (`RegistroComercioRequestDTO.horarios`, `@NotEmpty @Valid`, al menos una franja obligatoria; validación de `horaCierre` posterior a `horaApertura` a nivel Service) y expuesta de solo lectura en `ComercioResponseDTO` (poblada en los 3 lugares que lo construyen: `ComercioService`, `AdministradorService` vía `CatalogoController`/`ComercioController`/`AdministradorController`); sin endpoint de edición de horarios ya cargados, pospuesto a una fase futura. Probado end-to-end contra la base real: Cliente registrado → login → `GET`/`PUT /clientes/perfil` → persistencia confirmada, intento de mandar `email`/`dni` en el `PUT` ignorado por el DTO; Comercio registrado con 3 franjas (2 días distintos + horario partido en el mismo día) → persistencia confirmada por `SELECT` directo → horarios completos confirmados en la respuesta de `AdministradorController` (pendientes), `ComercioController` (perfil propio) y `CatalogoController` (público, tras aprobar el comercio); `horarios: []` → `400`; `horaCierre` anterior a `horaApertura` → `400` sin persistir ninguna fila. Datos de prueba eliminados al finalizar. Ver `docs/DECISIONES.md`, 2026-07-20. |
| 16 | Frontend HTML/CSS/JS | 🔄 en curso — Tramo 1/9 cerrado: base (`css/styles.css`, `js/api.js`, `js/geografia.js`, `js/validators.js`) + pantallas de General/Auth/Registro, probadas contra el backend real; páginas de error reorganizadas a `frontend/errores/` (2026-07-20). Tramo 2/9 cerrado: Catálogo/Home de Cliente, Detalle de Comercio + modal de Producto, Perfil de Cliente (`catalogo.html`, `comercio-detalle.html`, `perfil.html`, `js/catalogo.js`, `js/cliente.js`) — C41 (editar dirección) pospuesta, sin endpoint real. Tramo 3/9 cerrado: Carrito y Checkout (`carrito.html`, `checkout.html`, `js/carrito.js`, `js/checkout.js`, más el stepper de "agregar al carrito" sumado a `js/catalogo.js`) — `ClienteResponseDTO.direccion` agregado como ampliación formal de alcance (ver §5bis y `docs/DECISIONES.md`) para que el checkout de delivery conozca la dirección real del cliente; 2 bugs reales de backend/frontend encontrados y corregidos (modal de conflicto de comercio sin `appendChild`, `ProductoService.limpiarCarritosActivos` sin resetear `Carrito.comercio` al vaciar por producto agotado). Tramo 4/9 cerrado: Pedidos y Notificaciones (`pedidos.html`, `pedido-detalle.html`, `notificaciones.html`, `js/pedidos.js`, `js/notificaciones.js`, más la campana de notificaciones sumada a `renderTopBar` en `js/catalogo.js`) — sin endpoint de detalle de pedido por id, resuelto reutilizando `GET /pedidos/cliente` (filtrado por JWT); `nombreComercio` resuelto vía `GET /catalogo/comercios`, mismo patrón que Tramos 2/3, sin ampliar el backend; intervalo de polling (15s) fijado en este tramo, la guía lo dejaba abierto; cobertura de Figma nula en este tramo (ni `get_metadata` funcionó, límite total del plan Starter, no solo de `get_design_context`). Tramo 5/9 cerrado: Comercio — dashboard y perfil (`comercio-dashboard.html` CO33, `comercio-perfil.html` CO28+CO29, `js/comercio.js`; `comercio-pendiente.html`/`comercio-rechazado.html` de la Fase 16 Tramo 1 ganan un guard real de estado) — endpoint nuevo `GET /pedidos/comercio/resumen-hoy` (`ResumenPedidosHoyResponseDTO`, regla de negocio con deuda técnica documentada: `EN_PREPARACION` cuenta como facturado/pedidos de hoy mientras el MVP no tenga `ENTREGADO`, `RECHAZADO` no suma nunca); `PedidoResponseDTO.nombreCliente` agregado (gap simétrico al de `nombreComercio` del Tramo 4, sin catálogo público de clientes para resolverlo del lado del cliente); guard de estado real `initComercioEstadoPagina` porque `SecurityConfig` no bloquea por `Comercio.estado`, solo por rol; intento de Figma (MCP + fallback de navegador) sin éxito por dos motivos independientes, documentado en detalle. Tramo 6/9 cerrado: Comercio — CRUD de productos (`comercio-productos.html` CO13, `comercio-producto-form.html` CO14+CO15, modales CO16/CO17 en `js/comercio.js`, `js/cloudinary.js` nuevo) — `GET /categorias`/`GET /tags` ampliados de ADMINISTRADOR-only a cualquier autenticado (`SecurityConfig`, mutaciones siguen ADMINISTRADOR-only) porque el Comercio no tenía forma de poblar el selector de categoría; límite de 5 imágenes por producto probado real (`409` en la 6ª); incidente real de edición accidental sobre el archivo de Figma investigado y descartado (capa "Vector" = ícono genérico, 1086 ocurrencias en el archivo; CO13-CO17 confirmadas íntegras vía historial de versiones); 2 reglas nuevas permanentes agregadas a §4 (12 y 13) sobre uso del navegador contra Figma; incidente de credenciales de Cloudinary perdidas al reiniciar el backend, recuperadas con el dueño del proyecto. Sin comentarios en ningún archivo de `frontend/` (regla transversal 11, §4). Tramo 7/9 cerrado: Comercio — pedidos recibidos (`comercio-pedidos.html` CO18, `comercio-pedido-detalle.html` CO19+CO21, modal CO20 en `js/comercio.js`) — lista de pedidos del comercio con chips de filtro por estado real (`Todos`/`Pendientes`/`En preparación`/`Rechazados`), ordenada por prioridad de estado (pendientes primero); detalle único que alterna contenido según `estado` real (mismo criterio que `pedido-detalle.html` del Cliente), con acciones "Aceptar pedido"/"Rechazar pedido" solo cuando `estado = PENDIENTE`; `EstadoPedido` confirmado sin ninguna transición posterior a `EN_PREPARACION` en el MVP (`PENDIENTE → EN_PREPARACION/RECHAZADO`, sin `ENTREGADO`) — CO21 queda deliberadamente de solo lectura, sin ningún botón ni texto que insinúe una acción que el backend no soporta; sin endpoint de detalle de pedido por id, resuelto reutilizando `GET /pedidos/comercio` (mismo patrón que Tramos 4/6); verificación cruzada real con el lado Cliente confirmada — aceptar/rechazar desde Comercio genera la notificación real que el polling de `notificaciones.js` (Tramo 4) levanta solo, sin recargar la pestaña del Cliente. Cobertura de Figma nula en este tramo (misma cuota agotada del plan Starter que Tramos 5/6; el fallback de navegador no pudo capturar el lienzo de Figma esta vez — timeout sistemático de la herramienta de captura, no un bloqueo de permisos — así que las 4 pantallas se construyeron replicando el sistema de diseño ya establecido, sin ninguna referencia visual nueva). Tramo 8/9 cerrado: Administrador — aprobación de comercios (`admin-dashboard.html` AD01, `admin-comercios-pendientes.html` AD02, `admin-comercio-detalle.html` AD03 + modales AD05/AD06 en `js/admin.js` nuevo) — primer tramo del rol Administrador, capturas de Figma adjuntas directamente por el usuario en vez de MCP/navegador (cuota y timeout ya documentados en Tramos 5-7, sin reintentar ninguno de los dos caminos por pedido explícito). 2 endpoints/DTOs nuevos, ambos admin-only, sin tocar `ComercioResponseDTO` compartido con el catálogo público/self-service: `GET /administrador/metricas` (`MetricasAdminResponseDTO`: comerciosPendientes, comerciosTotal, clientesTotal, categoriasActivas, tagsActivos — `comerciosTotal`/`clientesTotal` son conteos agregados, no un listado, porque no existe endpoint de listado completo de comercios/clientes, gap ya documentado en Fase 15 §2.4 AD07/AD11) y `ComercioPendienteResponseDTO` (reemplaza a `ComercioResponseDTO` en `listarComerciosPendientes`, suma `fechaRegistro`/`condicionIva`/`emailCuenta`). Gap real de modelo de datos detectado en este tramo: la sección "Representante Legal" (Nombre/DNI) de AD03 no tenía ningún dato real, así que se omitió por completo (sin campos vacíos ni "no disponible") y `emailCuenta` se sumó dentro de "Datos del Comercio" en su lugar. **Corregido retroactivamente el mismo día** (ver `docs/DECISIONES.md`, 2026-07-22): el representante de un Comercio es una `PersonaFisica` real, colgada de la misma `Persona` que su `PersonaJuridica` — sin agregar nada a `PersonaJuridica`, como propuso un primer diagnóstico ya corregido en el propio `docs/DECISIONES.md`. `RegistroComercioRequestDTO`/RC02 capturan ahora 5 campos del representante y AD03 muestra la sección con datos reales. Bug real de layout encontrado y corregido en el momento: los íconos SVG dentro de `.detail-row` no tenían tamaño explícito y rompían el layout de la sección "Modalidades de Entrega" (tamaño intrínseco por defecto del navegador) — agregado `.detail-row svg { width/height: 15px }`. Corrección de un placeholder del Tramo 1: `auth.js` redirigía a un Administrador logueado a `admin/pendientes.html` (subcarpeta, nunca construida) — corregido a `admin-dashboard.html`, mismo criterio flat-file que el resto de `frontend/` desde el Tramo 5. Bell de notificaciones deliberadamente omitido del header de Administrador pese a aparecer en la captura de Figma: `AD36` (Fase 15 §2.2) ya había confirmado que ningún `notificacionService.crear(...)` del proyecto apunta nunca a un usuario Administrador — un ícono de campana real pero permanentemente en cero, sin ninguna pantalla de centro de notificaciones para Admin en el catálogo de Fase 15, se consideró un elemento de UI sin función real, mismo criterio que CO21 (Tramo 7) de no dejar ningún control que sugiera una capacidad inexistente. Verificación cruzada obligatoria con el login de Comercio confirmada de punta a punta: 2 comercios de prueba registrados y resueltos desde la UI real de Administrador (uno aprobado, uno rechazado con motivo) → login de cada uno redirige correctamente a `comercio-dashboard.html`/`comercio-rechazado.html` según su nuevo estado real, confirmado tanto por `curl` contra `GET /comercios/perfil` como desde el navegador real. Aislamiento por rol probado (un usuario `COMERCIO` intentando acceder a cualquier pantalla de Administrador es redirigido a `login.html`) y confirmado que el Administrador ve la totalidad de comercios pendientes sin ningún filtro de tenant (`AdministradorService` nunca filtra por ningún id de administrador — a diferencia de Cliente/Comercio, es una consulta global por diseño). `ComercioResponseDTO` (self-service) y el catálogo público, hasta entonces compartiendo el mismo DTO, se separaron en esa misma corrección: `ComercioPublicoResponseDTO` nuevo para `CatalogoController`, sin `representante` — el DNI nunca viaja sin autenticación. Tramo 9/9 cerrado (último tramo de contenido de la Fase 16): Administrador — categorías y tags (`admin-categorias.html` AD15/AD16/AD17/AD19, `admin-tags.html` AD20/AD21/AD23, funciones nuevas en `js/admin.js`) — gap real de backend encontrado y resuelto con el usuario: "N productos asociados" (AD15/AD20) no tenía respaldo real, se sumó `cantidadProductos` a `CategoriaResponseDTO`/`TagResponseDTO` vía 2 finders nuevos (`ProductoRepository.countByCategoriaId`, `ProductoTagRepository.countByTagId`), mismo criterio que `MetricasAdminResponseDTO` del Tramo 8. Copy de AD19/AD23 reescrito con el usuario: la captura decía "no se puede deshacer" pero `CategoriaService.baja()`/`TagService.baja()` son reversibles (`activo=false`, `reactivar()` ya existe desde Fase 8) — texto final explica que se puede reactivar editando, sin bloqueo de borrado por uso (confirmado, misma decisión de diseño ya documentada en Fase 15 §2.2 para `AD18`/`AD22`, no un hueco nuevo). Reactivación resuelta reutilizando el toggle "activa/o" del modal Editar (sin botón dedicado en el menú de 3 puntitos). Bug real corregido: `.field__error svg` sin tamaño explícito (mismo patrón que el bug de `.detail-row svg` del Tramo 8), corrige de paso el ícono del modal de rechazo de comercio. Mapeo pantalla↔archivo y desvíos Figma-vs-backend por tramo en `docs/MAPEO-ARCHIVOS-TRAMO1.md`/`TRAMO2.md`/`TRAMO3.md`/`TRAMO4.md`/`TRAMO5.md`/`TRAMO6.md`/`TRAMO7.md`/`TRAMO8.md`/`TRAMO9.md` y `docs/DECISIONES.md`. **Verificación GLOBAL de cierre de Fase 16 corrida el 2026-07-22** (checklist original de la guía + catálogo de 87+1 pantallas de Fase 15): confirmado backend real en todas las pantallas revisadas, selector Provincia/Localidad funcionando (`js/geografia.js` importado por `js/auth.js`), Cloudinary end-to-end funcionando (`js/cloudinary.js` importado por `js/comercio.js`, flujo probado de punta a punta en Tramos 6/11), cero comentarios confirmado con `grep` sobre la totalidad de `frontend/` (no solo los archivos de este tramo), flujo completo registro→...→notificación recorrido manualmente en una sola sesión continua (`cliente.demo` → `Pizzas del Sur` → pedido `#21` → `comercio2.demo` acepta → notificación real recibida vía polling, sin errores de consola en ningún paso) — **2 gaps reales encontrados, no cerrados en este tramo:** pantallas `C42` y `CO27` (confirmadas IN en `docs/PANTALLAS-MVP-FASE15.md` §1, "Cliente" y "Comercio" respectivamente) sin ningún archivo `.html`/`.js` que las cubra y sin ninguna entrada de exclusión documentada en ningún tramo — a diferencia del resto de pantallas no construidas, que siempre tienen una razón registrada (exclusión de alcance, sin mecanismo real, o sin backend). Detalle completo en `docs/DECISIONES.md`, entrada de cierre global de esta fecha — **la fase no se cierra sola**, queda pendiente de que el usuario decida qué hacer con `C42`/`CO27` y confirme el resto del checklist. Tramos posteriores de corrección/pulido (16.10 en adelante, incluidos crop editor y ajustes varios de Cliente/Comercio) documentados en `docs/DECISIONES.md` y en sus propios `docs/MAPEO-ARCHIVOS-TRAMO16.N.md` — no repetidos acá para no inflar esta celda más de lo que ya está. **Tramo 16.21 (2026-07-29): corrección UI/UX del rol Administrador** — última ronda de pulido tras Cliente y Comercio, sobre las 5 pantallas ya construidas (headers pasan de fondo naranja a blanco con logo centrado en el dashboard o título naranja centrado en el resto; limpieza de `admin-comercio-detalle.html` con avatar circular real vía `pintarAvatarComercio`, sin pills de tipo/modalidad, toast verde unificado tras aprobar/rechazar; menú de 3 puntitos de categorías/tags reemplazado por lápiz de edición directo, toggle "activo" quitado del modal de creación). 2 pantallas nuevas, `admin-comercios.html` (comercios aprobados, con modal de detalle de solo lectura) y `admin-clientes.html` (listado de solo lectura), resueltas con 3 endpoints/DTOs nuevos: `GET /administrador/perfil` (`AdministradorResponseDTO`), `GET /administrador/comercios` y `GET /administrador/clientes` (`ClienteAdminResponseDTO` nuevo) — `ComercioPendienteResponseDTO` renombrado a `ComercioAdminResponseDTO` (ver §3) al dejar de ser exclusivo de pendientes, suma `fotoPerfilUrl`. Ver `docs/DECISIONES.md` y `docs/MAPEO-ARCHIVOS-TRAMO16.21.md` para el detalle completo — **pendiente de confirmación explícita de Diego antes de cerrarse**, mismo criterio que el resto de los tramos de corrección. **Tramo 16.23 (2026-07-30): correcciones puntuales sobre 3 roles** — scrollbar oculta + título centrado en el modal de `admin-comercios.html`; botón "Cerrar sesión" agregado a `admin-dashboard.html` (no existía ninguna forma manual de salir desde Admin); botón del editor de recorte simplificado a "Confirmar"; header de "Panel" en `comercio-dashboard.html` reducido a solo logo centrado + campana (sacado el ícono+nombre del comercio, exclusivo de esa pantalla, sin afectar Productos/Pedidos/Perfil); galería de fotos del modal de producto de Cliente (`comercio-detalle.html`) rediseñada a foto principal + tira de miniaturas con lupa/zoom para productos con más de 1 foto (una sola foto sigue igual que antes). Ver `docs/DECISIONES.md` y `docs/MAPEO-ARCHIVOS-TRAMO16.23.md` para el detalle completo — **pendiente de confirmación explícita de Diego antes de cerrarse**. **Tramo 16.24 (2026-07-30): 8 ajustes de pulido UI/UX sobre Cliente y Comercio** — reversión parcial de la galería de fotos de producto de `comercio-detalle.html` (16.23 volvió a `object-fit:cover` a ancho completo y sin lupa/visor de zoom, motivo estético; se mantiene la tira de miniaturas); capitalización estandarizada en `perfil.html`/`comercio-perfil.html`; espaciado de `.comercio-card__body` subido de `6px` a `10px` (ya era uniforme, ahora con más aire); header nuevo en `carrito.html` con avatar del comercio + "Vas a hacer un pedido a **{nombre}**" (reemplaza el `<h1>` con el nombre, evita duplicarlo); `pedidos.html` suma avatar de comercio por tarjeta y reemplaza los chips de color de estado por un componente nuevo compartido `.pedido-estado` (texto negro + puntito de color, `--positivo`/`--rechazado`/`--pendiente`, cubre los 3 valores reales de `EstadoPedido`) — mismo componente reusado en `comercio-pedidos.html` y "Pedidos activos" de `comercio-dashboard.html` (comparten renderer, un solo cambio cubre ambas); `index.html` ordena comercios abiertos primero (`.sort()` estable, aplicado después del filtro por chip); `pedido-detalle.html` usa el nombre real del comercio en los 3 mensajes de estado en vez de "El comercio...". Gap real de datos encontrado (no introducido por este tramo): un comercio que pasó a `RECHAZADO` después de haber tenido pedidos ya no resuelve nombre/foto en el historial del Cliente porque `GET /catalogo/comercios` solo devuelve `APROBADO` — cae al fallback `Pedido #N` ya existente desde el Tramo 4, decisión de backend pendiente. Ver `docs/DECISIONES.md` y `docs/MAPEO-ARCHIVOS-TRAMO16.24.md` para el detalle completo — **pendiente de confirmación explícita de Diego antes de cerrarse**. **Tramo 16.25 (2026-07-30): 7 ajustes de pulido UI/UX, continuación directa del Tramo 16.24** — punto 1 retoma el espaciado de tarjetas de `index.html` que el Tramo 16.24 no había resuelto realmente (había subido el `gap` de 6 a 10px sin efecto visible): causa real encontrada por inspección de `getBoundingClientRect` contra los 11 comercios reales de la base — 4 nombres largos envolvían el `<h3>` a 2 líneas, inflando esa fila y generando la sensación óptica de más aire pese a que el `gap` ya era uniforme; fix real es truncar el título a 1 línea con elipsis (`text-overflow:ellipsis`), no otro ajuste de número. Badge Abierto/Cerrado de `index.html` y `comercio-detalle.html` (misma clase CSS compartida entre ambas pantallas) pasa de pill con fondo a texto + puntito (verde/gris), vía función nueva `pintarEstadoComercio` en `catalogo.js`. `carrito.html` suma foto de producto a la izquierda de cada línea (resuelta vía `GET /catalogo/comercios/{id}/productos`, mismo patrón de no ampliar el backend) y el botón "Vaciar" pasa a ícono + texto vertical en naranja — bug real encontrado y corregido en el camino: el fetch nuevo de fotos rompía toda la pantalla con un carrito viejo de un comercio no aprobado (`404` sin manejar), corregido con `try/catch` que degrada a sin-foto en vez de crashear. `comercio-producto-form.html` suma texto de resolución recomendada (1200×900px) derivado del editor de recorte 4:3 ya existente (Tramo 16.22), no un número arbitrario. Aviso de "comercio cerrado" de `comercio-dashboard.html` pierde el fondo amarillo a favor del mismo criterio de puntito. Color de "Pendiente" en los puntitos de estado de pedido recalibrado de naranja a amarillo (`--color-warning` nueva) porque no se distinguía bien de "Rechazado" — cambio centralizado en `styles.css`, cubre `pedidos.html`/`comercio-pedidos.html`/dashboard sin duplicar código. **Limitación de entorno de esta sesión:** el panel del navegador no compositó frames (`screenshot` falla sistemáticamente) — toda la verificación de este tramo se apoya en lecturas reales del DOM (`getComputedStyle`/`getBoundingClientRect`), no capturas. Ver `docs/DECISIONES.md` y `docs/MAPEO-ARCHIVOS-TRAMO16.25.md` para el detalle completo — **pendiente de confirmación explícita de Diego antes de cerrarse**. **Tramo 16.26 (2026-07-30): correcciones sobre el Tramo 16.25 (6 puntos)** — tercera vuelta sobre el espaciado de tarjetas de `index.html`: confirmado contra el DOM real que el fix de 16.25 seguía activo sin overrides (no era un bug de aplicación), se probó una hipótesis nueva (gap asimétrico 8px/12px en vez de uniforme, compensando el peso visual distinto entre título+pills "duros" y texto de horario "blando") y se le dieron a Diego las 2 líneas exactas de `styles.css` para ajustar a mano si hiciera falta. Header de `carrito.html` simplificado: `renderTopBar` suma un parámetro `accion` opcional (retrocompatible) y el botón "Vaciar todo" se movió ahí, visible solo con productos en el carrito. Texto de resolución recomendada acortado. Aviso de "cerrado" del dashboard sin el sufijo redundante "Cerrado hoy". Empty state de "sin productos" de `comercio-detalle.html` reducido (sin ícono, texto y botón a tamaño secundario) sin tocar la función compartida `renderEmptyState` usada en otros 9 lugares. Métricas del dashboard rediseñadas de 3 tarjetas separadas a una sola `stats-bar` con 3 columnas y divisores sutiles. **Limitación de entorno confirmada de nuevo:** `screenshot` del navegador sigue sin funcionar en esta sesión — se usó la herramienta `visualize` para generar mockups reales (mismo CSS del proyecto) como paliativo en los puntos más difíciles de juzgar solo con números. Ver `docs/DECISIONES.md` y `docs/MAPEO-ARCHIVOS-TRAMO16.26.md` para el detalle completo — **pendiente de confirmación explícita de Diego antes de cerrarse**. **Tramo 16.27 (2026-07-30): correcciones sobre el Tramo 16.26 (3 puntos)** — bloque de comercio de `carrito.html` rediseñado a tarjeta centrada (avatar, label gris, nombre negro peso 600) con el mismo separador (`1px solid var(--color-border)`, valor literal reusado, no uno nuevo) que ya usaban los ítems del carrito entre sí; botón "Vaciar todo" del header pierde el ícono de tacho, queda solo texto. Stats bar del dashboard suma `box-shadow: var(--shadow-card)` para igualar visualmente a las tarjetas de "Pedidos activos" (el fondo ya coincidía desde el 16.26). Punto 3 (espaciado de tarjetas de `index.html`) resuelto como documentación pura a pedido de Diego, sin más código — las 3 reglas exactas (archivo, línea, propiedad, valor) quedaron registradas en `docs/DECISIONES.md` para que las ajuste él mismo. **Bug real encontrado y corregido en el camino:** `const label` duplicado en la misma función de `carrito.js` (`SyntaxError`, pantalla en blanco) — y un hallazgo importante sobre la propia herramienta de verificación: `node --check` no detecta redeclaración de `const` en archivos `.js` con `import`/`export` sin `package.json` de tipo módulo (autodetección ESM de Node.js "ciega" a ese caso puntual) — a partir de acá, verificar copiando a `.mjs` o confiando en la carga real en navegador. Ver `docs/DECISIONES.md` y `docs/MAPEO-ARCHIVOS-TRAMO16.27.md` para el detalle completo — **pendiente de confirmación explícita de Diego antes de cerrarse**. **Tramo 16.28 (2026-07-31): correcciones sobre el Tramo 16.27 (6 puntos)** — bloque de comercio de `carrito.html` corregido (el 16.27 le había agregado un fondo sólido por error de interpretación, se quita y queda solo con el separador). Ícono de estado de `pedido-detalle.html`/`comercio-pedido-detalle.html` rediseñado sin círculo, al doble de tamaño, mediante un componente nuevo compartido `renderPedidoEstadoHeader` (`catalogo.js`) que reemplaza la implementación duplicada que tenían `pedidos.js` y `comercio.js` — de paso corrige un bug real de color (el ícono de "en preparación" usaba naranja en vez de verde). Avisos de "agotado"/"comercio cerrado" en `comercio-detalle.html` pasan de banner amarillo sólido a puntito + texto gris. Tarjeta "Comercios Pendientes" del dashboard de Admin pierde el fondo rosado (ahora blanca, igual que el resto de tarjetas) y el círculo naranja grande del contador pasa a un puntito chico, sin número (el subtítulo ya existente ya dice la cifra en palabras). Headers de 6 pantallas de Administrador pasan de título naranja a negro (`admin-dashboard.html` excluido a propósito). Badges de estado de cliente en `admin-clientes.html` pasan a puntito, mapeo de color confirmado 1 a 1 contra el enum real `EstadoUsuario` (5 valores, sin estados adicionales) — Suspendido usa un tercer tono de gris de la paleta para diferenciarse del texto negro y del gris de Inactivo. **Verificación de sintaxis reforzada con copia `.mjs`** aplicada a los 5 archivos JS tocados, según el método descubierto en el Tramo 16.27. Ver `docs/DECISIONES.md` y `docs/MAPEO-ARCHIVOS-TRAMO16.28.md` para el detalle completo — **pendiente de confirmación explícita de Diego antes de cerrarse**. **Tramo 16.29 (2026-07-31): 3 correcciones finales — cierre de la ronda de pulido UI/UX (Tramos 16.21-16.29)** — puntito de "Inactivo" en `admin-clientes.html` pasa de gris medio a negro puro (`var(--color-text)`), distinguible del gris claro de "Suspendido"; ícono grande de estado de `pedido-detalle.html`/`comercio-pedido-detalle.html` pasa a ser siempre naranja de marca (antes variaba verde/amarillo/rojo por estado) y el motivo de rechazo de un pedido se suma al componente compartido `renderPedidoEstadoHeader` (`catalogo.js`) como puntito rojo + texto, reemplazando el último cartel con fondo de color de la app (el `.banner-error` viejo de rechazo); nombre de comercio duplicado eliminado de `pedido-detalle.html` (ya estaba en el mensaje descriptivo del header). Incidente resuelto en el camino sin arriesgar la cuenta: login de `admin@bajonea.ar` con la contraseña ya documentada quedó a un intento fallido más de bloquearse (`intentos_fallidos=2` arrastrado de una sesión previa) — resuelto reseteando el contador vía el flujo real de recuperación de contraseña bajo el perfil `test` temporal (misma contraseña, sin cambiarla), backend revertido a perfil default antes de continuar. Ver `docs/DECISIONES.md` y `docs/MAPEO-ARCHIVOS-TRAMO16.29.md` para el detalle completo — **con este tramo, a criterio de Diego, la ronda completa de pulido UI/UX del MVP (16.21-16.29) queda 100% finalizada, pendiente únicamente de su confirmación explícita del checklist**. |
| 17 | Testing E2E con Playwright | 🔄 en curso — cierre original del 2026-07-31 confirmado por Diego en su momento, pero **reabierta de hecho el 2026-09-02**: el trabajo nunca se commiteó (`testing/playwright/` seguía `??` en git) y el proyecto avanzó mucho desde entonces (migración a `bajonea_final`, enmienda §1bis) sin volver a correr la suite. Re-verificación completa esa fecha encontró y corrigió 7 regresiones reales de drift (detalle en `docs/DECISIONES.md`, entrada "Fase 17: re-verificación..."): seed de `admin@bajonea.ar` faltante en `bajonea_test` (V1 de `bajonea_final` es schema-only), rate limit real de firma de Cloudinary agotado por la suite (ahora configurable, default intacto), `fotoPerfilUrl`/`redesSociales` nuevos-obligatorios en el registro de Comercio, formato de teléfono `+549` más estricto, normalización a Title Case de nombres (`TextoUtils.aTitleCase`), 2 campos/pasos nuevos del wizard de Comercio que el spec 01 no cubría (tipo de comercio, tab de horario personalizado), y el rediseño de los chips de filtro del catálogo (por modalidad, no por tipo). Resultado reproducido dos veces desde reset limpio: **35/36 en verde**, mismo único fallo aceptado de siempre. **Pendiente de confirmación explícita de Diego sobre este estado actual** — el texto original de cierre del 2026-07-31 queda abajo como registro histórico de esa fecha, ya no describe el estado vigente de los specs 01/03/04/06/08 ni de `scripts/reset-db.mjs`/`application-test.properties`. **Texto histórico de cierre (2026-07-31), confirmado por Diego tras revisar el reporte HTML y el checklist de cierre en su momento.** Evidencia completa en `docs/DECISIONES.md` (entradas del 2026-07-31, incluidas las 2 entradas de cierre) y `docs/DATA-TESTID-FASE17.md`. Preparación: base `bajonea_test` (receta de reset de 3 pasos — Flyway hasta V15 → seed geográfico del ETL de Georef → Flyway resto — automatizada en `npm run test:reset`), Playwright instalado en `testing/playwright/` (TypeScript, proyecto `chromium`), `playwright.config.ts` con `baseURL` al frontend real (`localhost:5501`, auto-levantado vía `webServer`) y backend en perfil `test` de arranque manual (puerto `8080` compartido con desarrollo, uno a la vez). `data-testid`: 470 atributos en total sobre las 39 pantallas + 11 JS del frontend + `js/crop.js` (5 testids del editor de recorte, sumados al escribir el spec 06 — `js/cloudinary.js` confirmado sin ninguno aplicable, es un módulo de servicio sin DOM propio), convención kebab-case en español documentada en `docs/DATA-TESTID-FASE17.md`. **Resultado de la corrida consolidada final:** 9 specs, 36 tests, `--workers=1` contra `bajonea_test` recién reseteada, **35/36 en verde** — reporte HTML en `testing/playwright/playwright-report/index.html` (`npx playwright show-report`). El único fallo (`07-crud-categorias-tags`, editar categoría) es 100% reproducible corriendo la suite completa (no un flake — pasa 4/4 corriendo el archivo solo): bug real de layout preexistente en `admin-categorias.html`/`styles.css` (el FAB "+" de crear categoría tapa el botón de editar de la última fila cuando la lista de categorías es larga, cosa que solo pasa corriendo la suite completa porque los `beforeAll` de `04`/`05`/`06` ya sembraron categorías propias antes de que `07` arranque) — no corregido en este cierre — **aceptado por Diego como bug de UI menor del panel de Administrador (pantalla que solo él usa), pendiente de una corrección informal a futuro cuando lo considere oportuno, no como deuda de testing ni bloqueante del cierre.** Dos hallazgos reales del cierre, con su detalle completo en `docs/DECISIONES.md`: (1) `EstadoPedido` del MVP es exactamente `PENDIENTE → EN_PREPARACION / RECHAZADO` (sin `ACEPTADO` ni `LISTO`/`ENTREGADO` como estados separados) — `05-pedido-flujo-completo.spec.ts` cubre el 100% del ciclo real actual (aceptar + rechazar, uno por test), no una porción; (2) Bajoneá tiene sesión única por cuenta (`Sesion.activa`) — un login nuevo invalida el anterior de la misma cuenta, sea por UI o por API — adoptada como convención de test para specs futuros: todo el setup por API de una cuenta va antes que su login por UI dentro del mismo test. `06-crud-productos.spec.ts` sube imágenes reales a la cuenta de Cloudinary del proyecto (decisión explícita de Diego, no bypass), con fixture propio (`testing/playwright/fixtures/bajonea-e2e-producto.png`, generado sin dependencias externas) y nombres de archivo únicos para poder identificar y limpiar las imágenes de test. Ver `docs/DECISIONES.md` (entradas del 2026-07-31, incluida la de cierre) y `testing/playwright/README.md` para el detalle completo. |
| 18 | Integración final y checklist de cierre | ⬜ pendiente |

**Bloque de fases nuevas (proyecto completo, enmienda de alcance §1bis, 2026-08-27) — pendientes de definir orden e implementación, sin número asignado todavía:** RedSocial de Comercio; bloque de Extras (GrupoExtra/Extra/ProductoGrupoExtra/ItemCarritoExtra/DetallePedidoExtra); `usuario.foto_perfil_url`; Dueño/Empleado/EmpleadoComercio; MercadoPago (CuentaMercadoPago/ConfiguracionTarifa/Pago/NotaCredito); Soporte/Reclamo; HistorialEstadoUsuario/HistorialEstadoPedido; máquina de estados completa de Pedido. El orden de abordaje se define en una sesión de planificación aparte con Diego — ver propuesta borrador (no cerrada) en `docs/AUDITORIA-MODELO-DATOS-NUEVO.md` §13.

No se pasa a la fase siguiente sin cerrar el checklist de la fase actual (definido en la guía, sección correspondiente). Esta regla sigue aplicando tal cual a las Fases 0-18 del bloque MVP; el bloque de fases nuevas todavía no tiene checklists propios definidos.

**Qué se actualiza siempre al cerrar una fase**, además del checklist específico de esa fase:

1. Esta tabla de fases y cualquier otra sección de `CLAUDE.md` que la fase haya afectado (§3 estructura, §5 entidades, §7 seguridad, etc.).
2. `docs/DECISIONES.md` — una entrada cronológica propia por cada decisión real tomada (nunca una nota de paso mezclada dentro de otra entrada), con destino explícito para cualquier pieza diferida a una fase futura.
3. **Revisar si esta fase introdujo un patrón recurrente nuevo** (no una decisión puntual de una sola tabla/campo) y, si es así, reflejarlo en la skill correspondiente — `.claude/skills/generar-capa-crud/SKILL.md` para patrones de código/estructura, `.claude/skills/skill-validaciones/SKILL.md` para patrones de validación — **antes** de dar la fase por cerrada, con una referencia corta a la entrada de `docs/DECISIONES.md` donde se originó, para trazabilidad. Las skills son la guía de "cómo hacerlo de acá en adelante"; `docs/DECISIONES.md` sigue siendo el registro de "por qué se decidió así" — no duplicar el historial completo en la skill.

### 6.1 Checklist de cierre de la Fase 0 — ✅ cerrada

- [x] Estructura de carpetas (`.claude/skills/`, `.claude/agents/`, `.claude/hooks/`, `docs/`, `backend/`, `frontend/`, `postman/`, `testing/playwright/`) creada.
- [x] `CLAUDE.md` revisado y aprobado por el usuario.
- [x] `docs/DECISIONES.md` creado vacío con encabezado y una línea de propósito.
- [ ] Skills, agentes y hooks del Anexo A/B/C creados (se abordan más adelante, no bloqueó el cierre de la Fase 0).

### 6.2 Checklist de cierre de la Fase 1 — ✅ cerrada

- [x] `mvn clean compile` sin errores (`BUILD SUCCESS`).
- [x] Estructura estándar Maven (`src/main/java`, `src/main/resources`, `src/test/java`).
- [x] `pom.xml` con todas las dependencias pedidas (Web, Data JPA, MySQL Driver, Security, Validation, Lombok, springdoc-openapi-starter-webmvc-ui, Mail, flyway-core, flyway-mysql) + jjwt (api/impl/jackson) y Cloudinary SDK agregados a mano.
- [x] `application.properties` con placeholders de variables de entorno, sin credenciales reales.
- [x] `./mvnw spring-boot:run` arranca Tomcat en el puerto 8080 y llega hasta el intento de conexión a MySQL (falla con `Unknown database 'bajonea'`, código 1049 — esperado, se resuelve en la Fase 2).

### 6.3 Checklist de cierre de la Fase 2 — ✅ cerrada

- [x] `docs/modelo-mvp.md` aprobado (21 tablas, tres rondas de revisión).
- [x] Migraciones V1 a V10 generadas en `backend/src/main/resources/db/migration/`, revisadas por el usuario columna por columna contra `docs/modelo-mvp.md` antes de aplicarlas.
- [x] `./mvnw spring-boot:run` aplicó las 10 migraciones sin error (`Successfully applied 10 migrations to schema \`bajonea\`, now at version v10`) y la app terminó de arrancar (`Started BajoneaApplication`).
- [x] Verificado contra la base: 21 tablas de dominio presentes (`SHOW TABLES` en `bajonea`) + `flyway_schema_history`.
- [x] `flyway_schema_history` con exactamente 10 filas, las 10 con `success = 1`.
- [x] Ningún dato ni tabla del alcance completo (Pago, Reclamo, Sesion, Soporte, historiales, etc.) presente.

### 6.4 Checklist de cierre de la Fase 2bis — ✅ cerrada

- [x] `provincia` cargada completa (24 registros).
- [x] `localidad` cargada completa (4037 registros, sin duplicados).
- [x] Río Grande / Tierra del Fuego verificados presentes y correctos (incluyendo verificación de codificación UTF-8 byte a byte tras corregir un incidente de re-encoding durante la carga — ver `docs/DECISIONES.md`).
- [x] El script (`backend/scripts/etl-georef/etl-georef.mjs`) es re-ejecutable sin romper nada: corrido dos veces, mismos conteos (24 / 4037) en ambas corridas, sin errores.

### 6.5 Checklist de cierre de la Fase 3 — ✅ cerrada

- [x] Estructura de paquetes creada (`config/`, `enums/`, `validation/`, `dto/`, `entities/`, `repositories/`, `services/`, `controllers/`, `exceptions/`), con los 11 enums del MVP ya implementados (`docs/modelo-mvp.md` §1) y los placeholders de `config/` sin anotaciones de Spring (no alteran el arranque antes de sus fases).
- [x] `ApiResponse<T>` implementado (`mensaje` + `data`, constructor, `@Getter`).
- [x] `GlobalExceptionHandler` (`@RestControllerAdvice`) mapea `RecursoNoEncontradoException`→404, `ConflictoDeNegocioException`→409, `ValidacionException`→400, y `MethodArgumentNotValidException`→400 con el detalle de campos inválidos — todas las respuestas de error usan `ApiResponse<T>`.
- [x] `validation/` creado con un ejemplo de referencia completo (`@ValidarCuit` + `CuitValidator`, algoritmo módulo 11 de AFIP) y un `package-info.java` documentando que estas anotaciones son exclusivas de `dto/request/`, nunca de `entities/`.
- [x] `mvn clean compile` sin errores (`BUILD SUCCESS`, 27 archivos fuente).
- [x] `GET /api/v1/health` probado contra la app levantada: `{"mensaje":"Bajoneá backend operativo","data":null}` con status `200` (autenticado con las credenciales HTTP Basic que Spring Security autogenera por defecto — `SecurityConfig` real recién se implementa en la Fase 7, hasta entonces todos los endpoints quedan protegidos por el auto-config de Spring Security).

### 6.6 Checklist de cierre de la Fase 4 — ✅ cerrada

- [x] Las 21 entidades creadas y compilando (`mvn clean compile`, `BUILD SUCCESS`): las 20 del modelo MVP + `Persona` (nodo intermedio real de la jerarquía de herencia, incorporado en `docs/modelo-mvp.md`, nota de alcance 1) — más `ProductoTagId` como `@Embeddable` de apoyo, sin tabla propia.
- [x] Cadena `@MapsId` encadenada verificada: `Usuario` (raíz, `@GeneratedValue`) → `Persona` → `PersonaFisica`/`PersonaJuridica` → `Cliente`/`Administrador`, sin saltear el nodo `Persona`.
- [x] `ProductoTag` con `@EmbeddedId` (`ProductoTagId`) y `@MapsId("productoId")`/`@MapsId("tagId")` en sus dos `@ManyToOne` — única entidad del proyecto con clave compuesta (ver nota en §4.7).
- [x] `./mvnw spring-boot:run` levantado contra la base real (`bajonea`, MySQL vía XAMPP): arranca sin ninguna excepción de Hibernate (`Started BajoneaApplication in 5.661 seconds`). El riesgo marcado durante la Fase 4 (falso "wrong column type" de Hibernate al validar `ENUM` nativo de MySQL contra `@Enumerated(EnumType.STRING)`) **no se materializó** — `ddl-auto=validate` pasó limpio en las 21 entidades sin necesidad de ningún ajuste.
- [x] Verificado contra la base: `SHOW TABLES` en `bajonea` devuelve 21 tablas de dominio + `flyway_schema_history`, con correspondencia 1 a 1 exacta contra las 21 entidades (sin faltantes ni sobras) — ver tabla de mapeo en la sesión que cerró esta fase.
- [x] Todas las entidades cumplen las convenciones de §4.7/§4.8: `@Getter` de clase, `@Setter` campo por campo (nunca en `id`), `@EqualsAndHashCode(of = "id")`, `fetch = FetchType.LAZY` en toda relación, `@Table(name = "...")` explícito, cero comentarios.
- [x] `Producto` sin columna de imagen propia — la relación a `ImagenProducto` es `@OneToMany(mappedBy = "producto")`.

### 6.7bis Checklist de cierre de la Fase 7 — ✅ cerrada

Alcance ampliado respecto al plan original de la guía (login + JWT + roles) — ver `docs/DECISIONES.md`, 2026-07-17 "Enmienda formal de alcance del MVP": suma recuperación de contraseña, bloqueo de cuenta, reactivación de cuenta y la tabla `Sesion`. Checklist de la guía original (4 puntos) + verificaciones propias de la ampliación, todas probadas con `curl` contra la app levantada y la base real (usuario de prueba sembrado por SQL, sin `RegistroService` — Fase 8 — todavía no hay alta vía API):

- [x] Login exitoso devuelve un JWT válido con rol embebido (`sub`, `userId`, `rol`, `sesionId`).
- [x] Endpoint protegido devuelve `403` con rol insuficiente (probado CLIENTE contra ruta ADMINISTRADOR — mismo mecanismo que CLIENTE/COMERCIO que pide la guía).
- [x] Endpoint protegido devuelve `401` sin token, con token corrupto, y con token de firma válida pero sesión cerrada (caso propio de la ampliación, no estaba en el checklist original).
- [x] Endpoints públicos alcanzables sin token: login, verificación de email (catálogo y registro todavía no existen — Fase 8/9 — no probados por no tener código que probar).
- [x] Bloqueo tras 3 intentos fallidos: `409` en el 4° intento, `usuario.estado = BLOQUEADO` persistido.
- [x] Recuperación de contraseña completa: token generado y persistido pese a fallo de envío SMTP (Fase 10 pendiente), `estado → ACTIVO`, `intentos_fallidos → 0`, contraseña vieja deja de servir, cierre forzado de sesión activa verificado.
- [x] Reactivación de cuenta completa: login bloqueado en `INACTIVO` (`409`), reactivación por token, login vuelve a funcionar.
- [x] Logout real (no simbólico): cierra la `Sesion`, mismo JWT deja de servir en la siguiente request.
- [x] `./mvnw compile` → `BUILD SUCCESS` (122 archivos). App levantada 3 veces contra la base real durante la sesión de pruebas, sin errores de arranque ni de validación de schema.
- [x] Bug real encontrado y corregido durante las pruebas: `@Transactional` de `AuthService` deshacía el contador de intentos fallidos al lanzar `CredencialesInvalidasException` — resuelto con `noRollbackFor` (ver `docs/DECISIONES.md`).
- [x] `@Transactional(noRollbackFor = CredencialesInvalidasException.class)` verificado: acotado a esa única clase, ningún otro `RuntimeException` pierde su rollback por defecto. Repasados los 6 puntos donde se lanza esa excepción en `AuthService` — ninguno deja mutación parcial sin revertir cuando corresponde revertir.
- [ ] Sin probar todavía: envío real de email (bloqueado por SMTP, Fase 10), endpoints de registro (Fase 8/9 no implementadas), Swagger/OpenAPI describiendo estos endpoints (Fase 13).

Cierre confirmado por el usuario el 2026-07-17.

### 6.7 Checklist de cierre de la Fase 5 — ✅ cerrada

- [x] 8 `RequestDTO` implementados en `dto/request/`: `RegistroClienteRequestDTO`, `RegistroComercioRequestDTO`, `DireccionRequestDTO`, `CategoriaRequestDTO`, `TagRequestDTO`, `ProductoRequestDTO`, `ItemCarritoRequestDTO`, `PedidoRequestDTO`. `LoginRequestDTO`, `ImagenProductoRequestDTO`, `RechazoPedidoRequestDTO` y `AprobacionComercioRequestDTO` quedan diferidos a las fases que los consumen directamente (7/9 auth, 11 Cloudinary, 8/9 flujo de pedido y aprobación de comercio) en vez de crearse sin service/controller que los use.
- [x] 12 `ResponseDTO` implementados en `dto/response/` (más `ApiResponse<T>`, ya cerrado en la Fase 3): `UsuarioResponseDTO`, `DireccionResponseDTO`, `ComercioResponseDTO`, `ProductoResponseDTO`, `ImagenProductoResponseDTO`, `CategoriaResponseDTO`, `TagResponseDTO`, `CarritoResponseDTO`, `ItemCarritoResponseDTO`, `PedidoResponseDTO`, `DetallePedidoResponseDTO`, `NotificacionResponseDTO`. `ProvinciaResponseDTO`, `LocalidadResponseDTO` y `CloudinarySignatureResponseDTO` quedan diferidos por el mismo criterio (geografía y Cloudinary todavía sin service/controller propio).
- [x] Validaciones custom del catálogo (§5bis) aplicadas en las dos tandas de identidad ya cubiertas: `RegistroClienteRequestDTO` (`@ValidarNombrePropio`, `@ValidarDni`, `@MayorDeEdad`, `@ValidarTelefonoArgentino`, `@ValidarPasswordSegura`) y `RegistroComercioRequestDTO` (`@ValidarCuit`, `@ValidarTelefonoArgentino`, `@ValidarPasswordSegura`, `@Valid` anidado sobre `DireccionRequestDTO`).
- [x] Mapeo Entity↔DTO confirmado como manual (sin MapStruct), a resolver en los Services de la Fase 8 — ningún DTO de esta fase depende de un mapper generado.
- [x] `./mvnw compile` — `BUILD SUCCESS` (106 archivos fuente, Java 21) verificado el 2026-07-17 tras el corte de infraestructura que había dejado esta fase sin confirmar.

### 6.8 Checklist de cierre de la Fase 6 — ✅ cerrada

- [x] 21 Repository implementados en `repositories/`, uno por cada una de las 21 entidades de la Fase 4 (correspondencia 1 a 1 verificada por nombre de archivo), todos `JpaRepository<Entidad, PK>`.
- [x] Patrón de colección por defecto respetado (§4.1): queries derivadas que devuelven múltiples filas usan `List<T>` (ej. `ProductoRepository.findByComercioIdAndEstadoNot`, `findByComercioIdAndCategoriaId`).
- [x] `repositories/` queda "tonto" — solo queries derivadas por nombre de método o `@Query`, sin lógica de negocio (verificado en `ProductoRepository`, patrón consistente con el resto).
- [x] `./mvnw compile` — `BUILD SUCCESS` (mismo run que la Fase 5, ambas fases comparten los mismos 106 archivos fuente) verificado el 2026-07-17.

---

### 6.9 Checklist de cierre de la Fase 15 — ✅ cerrada

- [x] Las ~150 pantallas del proyecto completo (capas Figma, formato mobile) cruzadas una por una contra `CLAUDE.md` §1 y contra los endpoints/lógica de negocio reales (12 Controllers, `EstadoPedido`/`EstadoComercio`/`EstadoUsuario` de `docs/modelo-mvp.md`, `CatalogoService`, `CategoriaService`/`TagService`, `AdministradorService`, `AuthService`) — no solo contra la memoria de `CLAUDE.md`.
- [x] Catálogo cerrado documentado en [docs/PANTALLAS-MVP-FASE15.md](docs/PANTALLAS-MVP-FASE15.md): 87 pantallas IN (nombre exacto de capa), 66 OUT categorizadas en 4 motivos distintos (exclusión de alcance §1, sin mecanismo real verificado en código, búsqueda/filtro global inexistente, sin backend construido — nunca mezcladas como si fueran la misma razón).
- [x] 4 puntos genuinamente ambiguos (direcciones de cliente C39/C40/C41, búsqueda/filtros C07/C08, dashboard de Comercio operando CO03/CO05, pantallas sin backend AD07/08/11/12+G15/16) resueltos explícitamente con el usuario antes de cerrar el catálogo — ninguno asumido.
- [x] 1 hueco real de diseño detectado (no una pantalla mal clasificada): ninguna pantalla del listado original cubre el dashboard normal de un Comercio ya aprobado y operando sin lenguaje de MercadoPago — especificado funcionalmente en `docs/PANTALLAS-MVP-FASE15.md` §4.3 como `CO33` (nueva), a diseñar recién en la Fase 16.
- [x] Sin tocar Figma — el cruce es puramente documental, el usuario arma la página nueva a mano copiando las pantallas del checklist.

---

## 7. Seguridad, JWT y autorización por rol (Fase 7 de la guía)

> Alcance ampliado respecto al plan original de la guía — ver `docs/DECISIONES.md` (2026-07-17, "Enmienda formal de alcance del MVP"). El JWT deja de ser puramente stateless: incorpora un claim `sesionId` validado contra la tabla `Sesion` en cada request.

- **`JwtService`:** genera el token al login (claims: `sub`=email, `rol`, `userId`, `sesionId`, expiración), lo valida y extrae claims. Usa `jwt.secret` y `jwt.expiration-ms` de `application.properties`.
- **`JwtAuthenticationFilter`:** `OncePerRequestFilter` que lee `Authorization: Bearer <token>`, valida la firma vía `JwtService`, y **además** consulta `SesionRepository` para confirmar que la `Sesion` del claim `sesionId` sigue con `activa = true` — si la firma es válida pero la sesión fue cerrada (bloqueo, recuperación/cambio de contraseña, login concurrente, logout), responde `401` igual. Si todo es válido, carga el `Authentication` en el `SecurityContextHolder` con el rol como `GrantedAuthority` (`ROLE_CLIENTE`, `ROLE_COMERCIO`, `ROLE_ADMINISTRADOR`).
- **Endpoints públicos (sin auth):** `POST /api/v1/auth/registro/cliente`, `POST /api/v1/auth/registro/comercio`, `POST /api/v1/auth/login`, `GET /api/v1/auth/verificar/{token}`, `POST /api/v1/auth/recuperar-password`, `POST /api/v1/auth/recuperar-password/confirmar`, `POST /api/v1/auth/reactivar-cuenta`, `POST /api/v1/auth/reactivar-cuenta/confirmar`, `GET /api/v1/catalogo/**`, `GET /api/v1/geografia/provincias`, `GET /api/v1/geografia/localidades`, Swagger UI, `/api/v1/test/**` (Fase 14 — listado en `RUTAS_PUBLICAS` pero solo alcanzable bajo `spring.profiles.active=test`, ver §9 y `docs/DECISIONES.md`).
- **Endpoints por rol:** `/api/v1/productos/**` (COMERCIO), `/api/v1/administrador/**` (ADMINISTRADOR), `POST`/`PUT`/`DELETE`/`/reactivar` de `/api/v1/categorias/**` y `/api/v1/tags/**` (ADMINISTRADOR), `/api/v1/carrito/**`, `/api/v1/pedidos/cliente/**` y `/api/v1/clientes/**` (CLIENTE, este último agregado en la Fase 16a), `/api/v1/pedidos/comercio/**` (COMERCIO). El cambio de contraseña desde perfil (`POST /api/v1/auth/cambiar-password`) requiere cualquier rol autenticado. `GET /api/v1/categorias/**` y `GET /api/v1/tags/**` quedan `authenticated()` sin restricción de rol desde la Fase 16 Tramo 6 (matcher específico de método `GET`, evaluado antes que el matcher genérico de ADMINISTRADOR) — el Comercio necesita poblar el selector de categoría/tags al crear o editar un producto, ver `docs/DECISIONES.md`.
- `sessionCreationPolicy(SessionCreationPolicy.STATELESS)` — sin cambios: esto es sobre el `HttpSession`/cookie de servlet de Spring Security, no sobre la tabla `Sesion` propia de la aplicación (ver nota de alcance arriba). No hay contradicción entre ambas.
- CORS habilitado para el origen del frontend durante desarrollo.
- Passwords hasheadas con `BCryptPasswordEncoder` (bean único).
- Logout (`POST /api/v1/auth/logout`, autenticado) deja de ser simbólico: cierra la `Sesion` activa del usuario (`activa = false`, `tipo_cierre = MANUAL`) — resuelve el pendiente que estaba abierto en la sección 9.

### 7.1 `application.properties` — claves relevantes (Fase 1.3)

```properties
server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/bajonea_final
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
jwt.secret=${JWT_SECRET:cambiar-en-produccion-por-variable-de-entorno}
jwt.expiration-ms=86400000
```

Usuario/contraseña de MySQL, SMTP y las credenciales de Cloudinary se leen siempre de variables de entorno (`${DB_USER}`, `${DB_PASSWORD}`, `${SMTP_HOST}`, etc.) — nunca valores reales en este archivo ni en el repo.

---

## 7bis. Contrato real de endpoints — deviaciones confirmadas contra la sección 9.3 de la guía

El inventario de Controllers de la Fase 9 (2026-07-18) encontró 3 endpoints donde el path o el verbo HTTP real difiere del literal de la sección 9.3 de `GUIA-IMPLEMENTACION-MVP-BAJONEA.pdf`. Las 3 quedan **confirmadas como decisión de diseño, no como pendiente de renombrar** — el path/verbo real de cada una es la fuente de verdad de acá en adelante, esta sección existe precisamente para que una auditoría futura no las vuelva a marcar como deviación sin revisar antes esta nota. Motivo de por qué se dejan así: son decisiones ya justificadas en `docs/DECISIONES.md` en el momento en que se construyó cada Controller (no accidentes de esta sesión), y no hay ningún consumidor real (Postman, frontend) todavía construido que dependa del path literal de la guía — cambiarlos ahora no evita romper nada existente, solo generaría trabajo sin beneficio.

| Controller | Guía (9.3) | Real | Motivo |
|---|---|---|---|
| `AdministradorController` | `PUT /administrador/comercios/{id}/resolucion` | `PUT /administrador/comercios/{id}/resolver` | Nombre de acción en infinitivo (`resolver`), consistente con el resto de los paths de mutación del proyecto (`/reactivar`, `/aceptar`, `/rechazar`) — la guía usa un sustantivo (`resolucion`) que rompe ese patrón. |
| `PedidoController` | `POST /pedidos` (crear) | `POST /pedidos/cliente` | Segmento de rol en la URL, mismo patrón que `/carrito` (CLIENTE) vs `/pedidos/comercio/**` (COMERCIO) ya usado en `SecurityConfig` — permite que la regla de autorización por rol matchee por prefijo de path sin depender de lógica adicional en el Controller. |
| `PedidoController` | `PATCH /pedidos/{id}/resolucion` (1 endpoint, body `aceptar` + `motivo`) | `PUT /pedidos/comercio/{id}/aceptar` + `PUT /pedidos/comercio/{id}/rechazar` (2 endpoints) | Decisión tomada al construir `PedidoService`/`PedidoController` (`docs/DECISIONES.md`, 2026-07-17): mismo criterio de nombrado `<Acción><Recurso>RequestDTO` ya establecido para `RechazoPedidoRequestDTO` — dos endpoints con intención explícita en el nombre en vez de un flag `aceptar: boolean` genérico. `PUT`, no `PATCH`, porque cada uno reemplaza el estado completo del recurso a un valor conocido, no aplica un parche parcial. |
| `NotificacionController` | `PATCH /notificaciones/{id}/leida` | `PUT /notificaciones/{id}/leida` | Mismo criterio que el resto del proyecto: `PUT` para una transición de estado completa y conocida (`leida: false → true`), reservando `PATCH` para cuando en el futuro haga falta una actualización parcial genuina (no hay ningún caso así todavía en el proyecto). |

**Endpoints agregados que la guía no lista en 9.3 pero sí exige en secciones de Service** (no son deviaciones, son adiciones justificadas): `AuthController` suma 5 endpoints de la ampliación de alcance de Fase 7 (recuperación de contraseña, bloqueo, reactivación — ver §7); `NotificacionController` suma `GET /notificaciones/no-leidas/contador`, exigido por 8.7 ("para el badge de polling") aunque 9.3 no lo liste explícitamente; `ProductoController` suma `POST /productos/{id}/cloudinary/firma`, exigido por 11.3 aunque 9.3 (escrita antes de la Fase 11) no lo liste; `ComercioController` suma `POST /comercios/perfil/foto/firma` + `PUT /comercios/perfil/foto` (Fase 11) — flujo de foto de perfil separado del de la galería de producto, sin límite de cantidad, confirmado con el usuario antes de implementarlo (`docs/DECISIONES.md`, 2026-07-18) porque `docs/modelo-mvp.md` ya marcaba `Comercio.foto_perfil_url` como "a incluir en el alcance del MVP" desde la Fase 2, distinta de la galería de `ImagenProducto`; `PedidoController` suma `GET /pedidos/comercio/resumen-hoy` (Tramo 16.5, dashboard de Comercio) — no exigido por ninguna sección de la guía original (escrita antes de que existiera el dashboard operativo de Comercio, hueco de diseño cerrado recién en la Fase 15/16.5), pedido explícito del dueño del proyecto para esta pantalla puntual; `AdministradorController` suma `GET /administrador/perfil`, `GET /administrador/comercios` y `GET /administrador/clientes` (Tramo 16.21) — mismo criterio, huecos de diseño cerrados recién al construir `admin-dashboard.html`/`admin-comercios.html`/`admin-clientes.html`, sin equivalente en 9.3 (`MetricasAdminResponseDTO`, único endpoint agregado antes para este Controller, solo daba conteos agregados, nunca el listado de filas).

---

## 8. Dónde vive cada cosa

- **Plan completo, detalle de cada fase, checklists de cierre:** `GUIA-IMPLEMENTACION-MVP-BAJONEA.md` (fuente de verdad).
- **Decisiones puntuales tomadas durante la implementación** (con fecha y justificación): [docs/DECISIONES.md](docs/DECISIONES.md).
- **Diccionario de datos recortado del MVP** (columnas, tipos, PK/FK): `docs/modelo-mvp.md` (se genera en la Fase 2).
- **Análisis de riesgo de concurrencia/transacciones por módulo, criterio de qué se implementa en el MVP vs. v2:** [docs/CONCURRENCIA-Y-TRANSACCIONES.md](docs/CONCURRENCIA-Y-TRANSACCIONES.md) (Fase 8, aprobado 2026-07-17). Consultar antes de escribir cualquier Service nuevo que mute estado compartido (aprobaciones, pedidos, imágenes, carrito).
- **Skills, agentes y hooks del asistente:** `.claude/skills/`, `.claude/agents/`, `.claude/hooks/` (detalle completo en Anexos A, B y C de la guía).
- **Backend:** `backend/` (Spring Boot, Fase 1 en adelante).
- **Frontend:** `frontend/` (HTML/CSS/JS vanilla, Fase 16).
- **Colecciones de API:** `postman/` (Fase 14).
- **Specs E2E:** `testing/playwright/` (Fase 17).

---

## 9. Pendientes de confirmar

- ~~Proveedor de email para verificación de cuenta (Fase 10)~~ — resuelto: Brevo quedó descartado por un bloqueo de activación de cuenta sin solución posible (ver `docs/DECISIONES.md`, 2026-07-20); se migró a Resend, probado de punta a punta con email real, Postman y Swagger UI. Fase 10 cerrada.
- ~~Recuperación de contraseña~~ — resuelto: sumado al MVP en la Fase 7 mediante enmienda formal de alcance (ver `docs/DECISIONES.md`, 2026-07-17), junto con bloqueo de cuenta y reactivación de cuenta.
- ~~Endpoint de logout real vs. simbólico~~ — resuelto: con la reincorporación de `Sesion` en la Fase 7, el logout es real (cierra la `Sesion` activa), no simbólico. Ver §7.
- ~~Endpoint auxiliar de bypass de verificación para testing~~ — resuelto: implementado en la Fase 14 como `TestController`/`TestSupportService` (`@Profile("test")`), `GET /api/v1/test/token-verificacion?email=` (verificación de cuenta) y `GET /api/v1/test/token?email=&tipo=` (generalizado, cualquier `TipoToken` — usado también para fijar una contraseña de admin conocida vía el flujo real de recuperación, sin tocar la base a mano). Permanente bajo el perfil `test`, no tiene fecha de remoción — ver `docs/DECISIONES.md`, 2026-07-19.
- **Orden e implementación de los tramos nuevos del proyecto completo (§1bis)** — pendiente de una sesión de planificación aparte con Diego. La auditoría (`docs/AUDITORIA-MODELO-DATOS-NUEVO.md`) ya identificó las piezas y un borrador de orden en su §13, pero no hay todavía números de fase ni checklists de cierre definidos para ninguno de esos tramos.
- ~~Migración baseline de Flyway para `bajonea_final` — gap de `dueno.persona_fisica_id`~~ — resuelto: confirmado en el mini-tramo de infraestructura del 2026-09-02 (versionado de archivos sueltos + actualización de `bajonea_test`, ver `docs/DECISIONES.md`) que `V1__baseline_bajonea_final.sql` ya tiene la columna `persona_fisica_id` en `dueno` — el gap detectado el 2026-08-27 ya no está presente en el archivo.
- ~~Unificación de `@ValidarDni`/`@ValidarFormatoDni`~~ — resuelto, pero no por unificación: la migración de "2. Legales" (`RegistroComercioRequestDTO.dniRepresentante`) ya había pasado a `@ValidarFormatoDni` antes de este punto, dejando `@ValidarDni` sin ningún uso real en el proyecto. Confirmado por `grep` completo (no solo `dto/request/`, incluidos tests) el 2026-09-01 y eliminada la anotación junto con `DniValidator` — ver `docs/DECISIONES.md`, entrada del 2026-09-01 "Limpieza de `@ValidarDni`...".
- **Tramo aparte, futuro, dedicado a mensajes de validación en inglés sin traducir** — reducido el 2026-09-01 (ver `docs/DECISIONES.md`, misma entrada de arriba): de los DTOs originalmente relevados, `DireccionRequestDTO`, `RegistroComercioRequestDTO`, `ItemCarritoRequestDTO` y `ActualizarCantidadItemCarritoRequestDTO` ya quedaron con mensaje en español en todos sus campos (Partes B/C de esa entrada), igual que `ImagenProductoRequestDTO`, `OrdenImagenRequestDTO`, `CambioEstadoProductoRequestDTO`, `PedidoRequestDTO` y `RedSocialRequestDTO` (relevados en una auditoría posterior más amplia, no listados en la cuenta original de 41/12). Quedan sin corregir: `ProductoRequestDTO` (auditado en detalle, ver Parte D de esa misma entrada — nombre/descripcion/precio sin mensaje, pendiente de definir criterios de validación antes de implementar), `ComercioPerfilRequestDTO`, `ClienteEditarPerfilRequestDTO`, `CategoriaRequestDTO`, `TagRequestDTO`, `RechazoPedidoRequestDTO`, `AprobacionComercioRequestDTO` — todavía sin fecha ni número de tramo asignado.
- ~~`spring.datasource.url` apuntando a `bajonea`~~ — resuelto: `application.properties` ya apunta a `bajonea_final`, la base definitiva del proyecto.
